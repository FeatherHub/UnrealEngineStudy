UEngine
└─ FWorldContext
   └─ ActiveNetDrivers : TArray<FNamedNetDriver>

UWorld
├─ NetDriver ──────────────> UNetDriver
└─ DemoNetDriver ──────────> UDemoNetDriver

UNetDriver
├─ 서버: ClientConnections[] ──> UNetConnection(클라이언트별)
└─ 클라이언트: ServerConnection ──> UNetConnection(서버 방향)
                                          └─ OpenChannels[]
                                             ├─ UControlChannel
                                             ├─ UActorChannel (고전)
                                             └─ UDataStreamChannel (Iris)

---

UWorld
 └─ UNetDriver
     ├─ 서버: ClientConnections[각 클라이언트]
     │   └─ UNetConnection
     │       ├─ PackageMap / Net GUID 매핑
     │       ├─ Control Channel
     │       ├─ Actor Channel ─ 액터 A의 이 연결용 복제 상태
     │       ├─ Actor Channel ─ 액터 B의 이 연결용 복제 상태
     │       └─ Voice Channel
     │
     └─ 클라이언트: ServerConnection[서버 한 곳]
         └─ UNetConnection

---

UNetDriver
  ServerConnection
  ClientConnections

  고전 복제 공유 데이터
    FNetworkObjectList NetworkObjects
    FNetGUIDCache GuidCache
    RepLayoutMap
    UReplicationDriver

  Iris
    UReplicationSystem
  
  프레임 진입점
    TickDispatch()  : 수신
    TickFlush()     : 복제 update와 송신
    PostTickFlush() : Iris PostSendUpdate

`GuidCache/FNetworkGUID`는 Iris 복제에는 사용되지 않는다

---

UNetDriver
  InitConnect: 클라이언트 모드로 서버에 접속
  InitListen: 서버 모드로 수신 대기
  TickDispatch: 들어온 패킷을 읽고 연결·채널에 전달
  TickFlush: 복제할 액터를 골라 내보내고, 쌓인 데이터를 패킷으로 보낸다
  ServerReplicateActors: 서버가 연결별 액터 복제를 스케줄
  ProcessRemoteFunction: RPC를 네트워크 메시지로 바꾸는 입구

---

`UNetDriver`는 연결 집합, transport 구현, 복제 시스템 진입점을 묶는 월드 단위 coordinator

Iris가 활성화되면 `TickFlush`는
`UpdateIrisReplicationViews()`와 `ReplicationSystem->NetUpdate()`를 호출

`TickFlush → ServerReplicateActors`는 고전 서버 복제일 때의 경로

`ReplicationDriver`는 복제 정책 확장점
`UReplicationGraph`가 대표 구현
Iris의 정책은 `UReplicationSystem`의 Filtering/Prioritization 쪽에

---

[수신 단계]
UNetDriver::InternalTickDispatch
  → UReplicationSystem::PreReceiveUpdate
  → UNetDriver::TickDispatch // socket/transport에서 packet 수신
  → UReplicationSystem::PostReceiveUpdate

[송신 단계]
UNetDriver::TickFlush
  → 고전: ServerReplicateActors
  → Iris : UpdateIrisReplicationViews, UReplicationSystem::NetUpdate

---

UNetDriver
  UDemoNetDriver
  replay 기록·재생용 가상 네트워크 driver

  UIpNetDriver
  IP/UDP 기반

---

UObject
UPlayer
UNetConnection
  자신을 관리하는 UNetDriver
  OwningActor
  ViewTarget - 이 연결의 플레이어가 현재 세상을 관찰하는 기준점으로 삼는 액터

  UPackageMap - 원격과 객체 참조를 합의하는 맵 // UPackageMapClient

  RemoteAddr, URL원격 주소와 접속 정보
  State - 연결·닫힘 등의 연결 상태
  
  Channels - 채널 번호로 접근하는 채널 배열
  FActorChannelMap ActorChannels - AActor -> UActorChannel

  Handler: PacketHandler - 송수신 패킷을 가공하는 패킷 핸들러
  
---

Beacon 연결에서는 Beacon 계열 Actor가 소유자가 될 수 있다

---

UNetDriver가 FSocket을 소유

서버면 리슨 소켓 하나

수신: UDP는 connectionless라 모든 클라가 그 한 소켓으로 들어와
NetDriver가 소켓을 읽어 보낸 주소로 구분(demux) 해서 
맞는 UNetConnection으로 라우팅(새 주소면 새 connection 생성)

---

송신: UNetConnection(IpConnection)이 자기 remote 주소로 SendTo 를 그 소켓에 호출
즉 소켓은 드라이버 소유지만, 보낼 땐 connection이 자기 주소를 얹어 그걸 통해 내보내.

---

IpConnection ↔ IpNetDriver 커플링

NetDriver는 NetConnectionClass(어떤 UNetConnection 서브클래스를 만들지)를 갖고
UIpNetDriver는 UIpConnection을 만들어

transport마다 드라이버+커넥션이 한 쌍으로 커플링

UIpNetDriver ↔ UIpConnection
UDemoNetDriver ↔ UDemoNetConnection
Steam / EOS / WebSocket 계열 — 각자 드라이버+커넥션 쌍

---

Beacon

정식 게임 세션(전체 월드 복제)을 안 열고, 특정 목적으로만 서버와 대화

AOnlineBeaconHost
AOnlineBeaconClient

용도: 접속 전 자리 예약(reservation), 매치메이킹 핸드셰이크, 로비/파티 상태 조회

---

FHttpModule / IHttpRequest

NetDriver/NetConnection/FSocket 스택과 완전히 별개 서브시스템

FHttpModule::Get().CreateRequest()
  → SetURL / SetVerb(GET·POST) / 헤더 / 바디
  → ProcessRequest()        // 비동기
  → OnProcessRequestComplete 콜백으로 응답

플랫폼 HTTP 구현(libcurl 등) 위 TCP/HTTP로 나가
소켓도 커넥션도 공유 안 해

게임 클라 ──HTTP(FHttpModule)──▶ 백엔드 웹 API ──▶ DB

---
---
---

UNetConnection::InitBase
└─ Driver/URL/mode/packet state 초기화

ReceivedRawPacket
├─ PacketHandler::Incoming (설정된 경우)
└─ ReceivedPacket
   └─ FInBunch로 분리 → channel dispatch

UChannel::SendBunch
└─ UNetConnection::FlushNet
   └─ packet 확정 → PacketHandler::OutgoingHigh → LowLevelSend

Connection에 쌓인 outgoing bunch를 packet 경계로 확정해 하위 transport로 내린다

---

ReceivedRawPacket
handler 적용 전의 raw 진입점

ReceivedPacket
Unreal packet header와 bunch를 해석

---

UNetConnection packet
├─ packet header / sequence / ACK
└─ 여러 Bunch
   └─ 각 FInBunch/FOutBunch의 ChIndex와 ChName

송신: FOutBunch → packet에 병합
수신: packet parse → FInBunch → 해당 UChannel

---

번치 

FNetBitWriter
└─ FOutBunch
   └─ FControlChannelOutBunch

FNetBitReader
└─ FInBunch

특정 Channel 번호를 가진 논리 메시지 조각
하나의 패킷에 여러 번치가 들어갈 수 있다
Bunch는 Channel의 논리적 데이터 단위
한 packet에 여러 Bunch가 들어갈 수 있고, 큰 Bunch는 partial bunch로 여러 packet에 나뉠 수 있다

---

UChannel
├─ UControlChannel     : handshake/control message
├─ UActorChannel       : 고전 Actor/subobject 상태와 RPC
├─ UVoiceChannel       : voice data
└─ UDataStreamChannel  : Iris DataStream 운반


UChannel
  UNetConnection
  ChIndex: 이 연결 안에서의 채널 번호

  OpenAcked / Closing / Dormant / OpenTemporary / Broken: 채널 수명 상태

  FInBunch* InRec / FOutBunch* OutRec
  FInBunch* InPartialBunch

  Init: 연결과 채널 번호를 붙인다
  ReceivedBunch: 해당 채널용 수신 메시지를 처리한다
  SendBunch: 송신 번치를 연결의 패킷 버퍼에 넣는다
  ReceivedAck, ReceivedNak: 확인 또는 손실을 처리

---

Channel은 하나의 Connection 안에서 논리적 데이터 흐름을 분리한다
파생 Channel마다 Bunch payload의 의미가 다르고

base `UChannel`은
reliable sequence, partial bunch 조립 같은 공통 전송 규칙을 제공한다

---

UActorChannel
  Actor - 이 채널이 담당하는 액터
  ActorNetGUID - 현재 client에서만 유효

  ActorReplicator - 액터 자신의 프로퍼티 복제기
  TMap<UObject*, TSharedRef<FObjectReplicator>> ReplicationMap - 액터의 복제 서브오브젝트별 복제기

  RelevantTime - 이 연결에서 마지막으로 relevant였던 시각
  LastUpdateTime - 마지막 복제 시각

  QueuedBunches - 객체 참조 해석을 기다리는 수신 번치
  FQueuedBunchObjectReference

---

[송신]
UActorChannel::ReplicateActor
├─ FObjectReplicator::ReplicateProperties (ActorReplicator)
├─ subobject replicator 순회
└─ FOutBunch 작성/전송

이미 선택된 Actor의 property/subobject payload를 만드는 실행 단계
액터와 서브오브젝트의 현재 상태를 번치로 직렬화

---

[수신]
UActorChannel::ReceivedBunch
├─ channel open / Actor spawn
├─ content block 분해
├─ FObjectReplicator::ReceivedBunch
└─ property/RPC 적용

프로퍼티 수신, RPC 수신을 분기한다

---

FRepLayout (schema/command layout)
   ├─ TArray<FRepParentCmd> Parents
   ├─ TArray<FRepLayoutCmd> Cmds

   ├─ lifetime condition / RepNotify

   ├─ CompareProperties
   ├─ SendProperties / ReceiveProperties
   └─ SendPropertiesForRPC / ReceivePropertiesForRPC

---

`UNetDriver` AP
GetObjectClassRepLayout, GetFunctionRepLayout, GetStructRepLayout

객체별 비교 상태 ──> FReplicationChangelistMgr / FRepChangelistState
연결별 송신 상태 ──> FRepState

---

FNoncopyable
├─ FReceivingRepState
├─ FSendingRepState
└─ FRepState

---

UActorChannel
├─ ActorReplicator
│  └─ FObjectReplicator
└─ ReplicationMap
   ├─ Subobject A ─────────> FObjectReplicator
   └─ Subobject B ─────────> FObjectReplicator

---
  
FObjectReplicator
├─ identity / object
│  ├─ FNetworkGUID 
│  ├─ UClass ObjectClass / UObject* ObjectPtr


├─ shared/per-connection state
│  ├─ TSharedPtr<FReplicationChangelistMgr> ChangelistMgr
│  ├─ TSharedPtr<FRepLayout> RepLayout
│  ├─ TUniquePtr<FRepState> RepState (이 Connection용)


├─ FRepState
│  ├─ TUniquePtr<FReceivingRepState>
│  │  ├─ StaticBuffer / GuidReferencesMap
│  │  └─ RepNotifies / RepNotifyMetaData

│  └─ TUniquePtr<FSendingRepState>
│     ├─ bOpenAckedCalled
│     ├─ HistoryStart / HistoryEnd / NumNaks
│     ├─ LastChangelistIndex / LastCompareIndex / CustomDeltaChangeIndex
│     ├─ RepFlags / RepChangedPropertyTracker
│     ├─ ChangeHistory[MAX_CHANGE_HISTORY] / Retirement / PreOpenAckHistory
│     ├─ LifetimeChangelist

├─ UNetConnection* / UActorChannel* 
├─ FOutBunch* RemoteFunctions
├─ RemoteFuncInfo / PendingLocalRPCs

---

UObject 하나를 특정 Connection에 복제하는 실행 객체

Connection별 ACK/history와 pending RPC, RepNotify, 미해결 참조 상태를 결합
동일 UObject에도 연결마다 별도 상태가 필요

이전에 보낸 값과 현재 값을 비교
변경된 프로퍼티를 직렬화
수신한 프로퍼티를 대상 객체에 적용
객체 참조가 아직 도착하지 않았으면 보류

---

FObjectReplicator
├─ lifecycle
│  ├─ InitWithObject / CleanUp
│  └─ StartReplicating / StopReplicating

├─ 송신·delivery
│  ├─ ReplicateProperties_r(FOutBunch&, FReplicationFlags, FNetBitWriter&)
│  ├─ PostSendBunch 

├─ 수신·reference
│  ├─ ReceivedBunch / ReceivedRPC
│  ├─ UpdateGuidToReplicatorMap / MoveMappedObjectToUnmapped
│  ├─ PostReceivedBunch / 

├─ RPC·notify/property payload
│  ├─ CallRepNotifies

└─ UObject receive hooks: PreNetReceive / PostNetReceive

---

UNetDriver
└─ TSharedPtr<FNetGUIDCache>
   ├─ UObject/path <-> FNetworkGUID

UObject
└─ UPackageMap
   └─ UPackageMapClient

UNetConnection
└─ TObjectPtr<UPackageMap>
   └─ game connection의 runtime instance: UPackageMapClient
      ├─ 직렬화 중 UObject ↔ NetworkGUID 변환
      ├─ export / must-be-mapped 처리
      └─ Driver의 FNetGUIDCache 사용

---

FNetworkGUID
packet 안의 UObject 참조 식별자

UPackageMapClient
Connection별 serialization 문맥을 제공
GUID↔object의 중심 cache는 Driver에 있어 연결들이 공유

Iris는 NetRefHandle/ObjectReference/Token 체계로 재구성
Iris에서 `GuidCache/FNetworkGUID`를 사용하지 않는다

---
---
---

UNetDriver::ServerReplicateActors
├─ ServerReplicateActors_PrepConnections
├─ ServerReplicateActors_BuildConsiderList
│  └─ FNetworkObjectList에서 candidate 수집
├─ Connection별 loop
│  ├─ relevance / dormancy / channel 조건
│  ├─ 우선순위 계산
│  └─ ServerReplicateActors_ProcessPrioritizedActorsRange
│     ├─ 필요 시 UActorChannel 생성
│     └─ UActorChannel::ReplicateActor


각 UNetConnection에 대해
  이 액터가 이 연결에 relevant한가?
  우선순위와 대역폭 여유가 있는가?
  채널이 없다면 UActorChannel 생성
    UActorChannel::ReplicateActor


Actor, Connection별 relevance, 전송 우선순위를 정한 뒤 Actor Channel의 serialization으로 내려간다
Replication Graph를 사용하면 후보 수집과 정책 일부가 `UReplicationDriver` 쪽으로 이동한다

---

UObject
└─ UReplicationDriver
   └─ UReplicationGraph
      ├─ TArray<TObjectPtr<UReplicationGraphNode>> GlobalGraphNodes
      ├─ TArray<TObjectPtr<UNetReplicationGraphConnection>> Connections
      └─ ServerReplicateActors(float) override


“어떤 액터를 어떤 연결에 보낼지”의 정책을 바꾸기 위한 확장점
Replication Graph는 이 기반을 이용해 후보 액터를 공간·팀·게임 규칙 단위로 빠르게 골라내는 전략

---

Net UFunction 호출
└─ UObject::ProcessEvent
   └─ UNetDriver::ProcessRemoteFunction
      └─ UActorChannel / FObjectReplicator
         └─ FOutBunch → UNetConnection

대상 UNetConnection과 UActorChannel 결정
함수 식별자 + 인자를 FOutBunch에 직렬화
전송

---

UNetConnection::ReceivedPacket
└─ UActorChannel::ReceivedBunch
   └─ FObjectReplicator::ReceivedRPC
         UObject::ProcessEvent
      └─ UFunction::Invoke / generated exec thunk
         └─ _Implementation

---
---
---

FObjectReplicator       changelist → 비트    FRepLayout 이 property↔비트 변환

UChannel                bunch                
UNetConnection          packet               bunch 들을 packet 으로 패킹, 대역폭 예산

PacketHandler           변형된 packet        handshake·암호화·압축 (HandlerComponent 체인)
FSocket                 UDP datagram         전송

(UIpNetDriver/UIpConnection)

UIpNetDriver/UIpConnection

---

UWorld
  FWorldContext 가 이름별 NetDriver 를 여러 개 보유 (GameNetDriver, DemoNetDriver, beacon)

UNetDriver                                        
├─ ServerConnection:UNetConnection*               // client 전용
├─ ClientConnections:UNetConnection*[]            // server 전용
├─ GuidCache:FNetGUIDCache*                       // NetGUID↔UObject. 모든 connection 공유
├─ NetworkObjects:FNetworkObjectList*             // 복제 대상 actor 집합
└─ ReplicationDriver:UReplicationDriver*          // 정책 교체점 — ReplicationGraph

---

UNetConnection : UPlayer                          // 원격 플레이어
├─ Channels:UChannel*[]                           // ChIndex 인덱싱. 0 = Control, 나머지 Actor/Voice
├─ PackageMap:UPackageMap*                        // UPackageMapClient — GUID export/ack 의 연결별 상태
├─ Handler:PacketHandler*
│   └─ HandlerComponents:HandlerComponent*[]      // StatelessConnect, 암호화, Oodle 압축
├─ PacketNotify:FNetPacketNotify                  // packet 수준 seq/ack/nak 창
├─ SendBuffer:FBitWriter                          // 조립 중인 outgoing packet
└─ OwningActor:AActor*                  

---

UActorChannel : UChannel                          
├─ Actor:AActor*
└─ ReplicationMap:<UObject*->FObjectReplicator>   // replicated subobject

---

FObjectReplicator                                 
├─ RepLayout:FRepLayout*                          // 클래스당 1개 공유
├─ RepState:FRepState*                            // 이 연결 전용의 송수신/ack history
└─ ChangelistMgr:FReplicationChangelistMgr*       // 객체당 1개 공유 — 값 비교는 프레임당 1회만

---
---
---

UWorld::Tick()
{
  this::BroadcastTickDispatch();       // 프레임 앞: 수신
  // gameplay 
  this::BroadcastTickFlush();          // 프레임 끝: 송신 — 각 UNetDriver::TickFlush()
}

UNetDriver::TickFlush()
{
  this::ServerReplicateActors()
  {
    this::ServerReplicateActors_PrepConnections();     
    this::ServerReplicateActors_BuildConsiderList();   // NetworkObjects 중 NextUpdateTime 도달·비-dormant 만

    for each connection
    this::ServerReplicateActors_PrioritizeActors();    // AActor::IsNetRelevantFor() 필터 + NetPriority × 경과시간 정렬
    this::ServerReplicateActors_ProcessPrioritizedActorsRange()
    {
      // 대역폭 예산 안에서 상위부터:
      UNetConnection*::CreateChannelByName();  // 첫 복제면 channel open — 클라쪽 spawn

      UActorChannel*::ReplicateActor()
      {
        FObjectReplicator::ReplicateProperties(Bunch)
        {
          // ChangelistMgr: 이번 프레임 값 비교는 객체당 1회 (모든 connection 공유)
          this::RepLayout::ReplicateProperties();  // changelist − (이 연결이 ack 한 것) → 비트 기록
        }
        // replicated subobject 도 ReplicationMap 의 replicator 로 동일 반복

        this::SendBunch(&Bunch);        // FOutBunch 가 Connection::SendBuffer 에 패킹
      }
    }
  }

  UNetConnection::SendRawBunch/FlushNet    
    this::Handler:PacketHandler*::Outgoing();   // 압축 → 암호화
    this::LowLevelSend();                       // socket 송신
}

---
---

PacketHandler
이 연결의 패킷을 변환하는 파이프라인

암호화: 커넥션마다 자기 키를 협상
핸드셰이크(StatelessConnect) 상태

---

connectionless용 PacketHandler

UNetDriver
└─ ConnectionlessHandler : PacketHandler*   ← (StatelessConnect 핸드셰이크)

아직 커넥션이 없는 주소에서 온 패킷을 처리
핸드셰이크가 끝나 커넥션이 생기면, 그 커넥션이 자기 per-connection 핸들러를 갖게 돼

---
---
---


UNetDriver::TickDispatch()
{
  UNetConnection*::ReceivedRawPacket(Data)
  {
    this::Handler:PacketHandler*::Incoming(); // 복호화·압축해제

    this::ReceivedPacket(Reader)
    {
      // PacketNotify: header 의 seq/ack 처리
      //   내 packet 의 nak → 거기 실렸던 reliable bunch 를 다음 packet 에 재수록 (UChannel::ReceivedNak)
      //   ack → replicator 의 "ack 된 changelist" 갱신

      // packet 내 FInBunch 분리 및 순회
      UChannel*::ReceivedRawBunch()
      {
        // reliable: channel 별 sequence 로 순서 보장, 결번이면 보류 큐에 대기
        // partial bunch 는 재조립 후 전달

        UActorChannel::ReceivedBunch()
        {
          // open bunch 면: UPackageMapClient* 가 NetGUID→archetype resolve → actor spawn

          FObjectReplicator::ReceivedBunch()
          {
            this::FRepLayout*::ReceiveProperties();  // 객체 메모리에 기록 + RepNotify 큐잉
            // RPC 필드면: ReceivedRPC() → UFunction 실행
          }
        }
      }
    }
  }
}

---
---
---

AActor::CallRemoteFunction()
  UNetDriver*::ProcessRemoteFunction(Actor, Function, Parms)
  {
    // Server/Client/NetMulticast 플래그 × 이 머신의 role → 방향·대상 connection 결정
    // 해당 actor 의 UActorChannel 확보 후, FRepLayout 이 파라미터 직렬화 → bunch 로 송신
    // reliable 여부 UFUNCTION 지정자가 bunch 플래그로
  }

호출을 네트워크로 보낼지 판단하는 입구

---

수신 exec thunk는 parameter를 풀고 `_Implementation`을 부른다

---

클라이언트                           서버
UPendingNetGame                       UWorld/UNetDriver
    │ NMT_Hello ----------------------->│  버전·플랫폼 확인
    │<------------------- NMT_Challenge │
    │ NMT_Login ----------------------->│
    │                                 AGameModeBase::PreLoginAsync
    │<--------------------- NMT_Welcome │  map 이름 등
    │ NMT_Netspeed -------------------->│  (map 로드, NetDriver를 UPendingNetGame → UWorld 로 인계)
    │ NMT_Join ------------------------>│
    │                                UWorld::SpawnPlayActor()
    │<-------------------------------Connection::PlayerController 결합


---
---
---

               UNetDriver
                     │
              FNetGUIDCache
          (공유 GUID↔object 사전)
                 ▲       ▲
                 │       │
UNetConnection A │       │ UNetConnection B
└─ PackageMap A ─┘       └─ PackageMap B
   (A용 export 상태)        (B용 export 상태)

---

FNetGUIDCache
  = UNetDriver 스코프의 객체 주민등록소
    “이 객체는 420번이다.”

UPackageMapClient
  = 클라이언트 한 명을 위한 전달·확인 장부
    “A 클라이언트는 420번이 Rifle임을 이미 확인했는가?”
    "원격은 그 식별자가 무엇을 가리키는지 이미 아는가?"
    Connection 문맥의 질문

---

UNetConnection
├─ PlayerController
│  └─ APlayerController::GetViewTarget()
├─ ViewTarget: AActor

AActor::IsNetRelevantFor(RealViewer, ViewTarget, SrcLocation)


PlayerController(연결의 주인)는 그대로지만 
관전자 카메라나 다른 Actor를 보는 경우
"지금 내 화면에 필요한 세계는 B 주변"

---
---
---

UNetDriver
└─ ReplicationSystem:UReplicationSystem*            // Iris 의 루트. NetDriver 당 1개 
     FReplicationSystemImpl
      ├─ FReplicationSystemInternal
           UReplicationBridge

           FReplicationProtocolManager
           FReplicationStateDescriptorRegistry

           NetRefHandleManager
           FDirtyNetObjectTracker
           FReplicationConnections

           FReplicationStateStorage // 중앙 quantized internal 상태 버퍼
           FChangeMaskCache
           UIrisObjectReferencePackageMap

           FReplicationFiltering
        ├─ FReplicationConditionals   
        ├─ FReplicationPrioritization 
           FNetObjectGroups
           FWorldLocations
           FDeltaCompressionBaselineManager
           
---
   
UNetConnection::Channels[ChIdx]
  └─ UDataStreamChannel
      ├─ UDataStreamManager
          └─ UReplicationDataStream
              ├─ FReplicationWriter
              └─ FReplicationReader

FReplicationConnections Connections
├─ TArray<FReplicationConnection> Connections
   └─ [ConnectionId]
      ├─ FReplicationWriter*
      ├─ FReplicationReader*
      ├─ UDataStreamManager
      ├─ FObjectPtr UserData

---
---
---

FReplicatedObjectData
├─ FNetRefHandle / FNetHandle
├─ FReplicationProtocol*
├─ FReplicationInstanceProtocol*

├─ uint8* ReceiveStateBuffer

├─ SubObjectRootIndex / SubObjectParentIndex

└─ uint16 Flags

---

SubObjectRootIndex    
이 서브오브젝트의 RootObject(최상위 루트=액터)의 index

SubObjectParentIndex  
이 서브오브젝트의 직속 Parent(한 단계 위)의 index

---
---
---

UEngineReplicationBridge::StartReplicatingActor(AActor*)
  등록을 오케스트레이션
  그 안에서 handle 발급은 manager에 위임

수신 시 CreateNetRefHandleFromRemote

---
---
---

FNetRefHandleManager
  ├─ FRefHandleMap RefHandleToInternalIndex
  ├─ FNetHandleMap NetHandleToInternalIndex
  ├─ AssignedInternalIndices // free internal index 탐색

  ├─ TNetChunkedArray<FReplicatedObjectData>
  
  ├─ GlobalScopableInternalIndices / AssignedInternalIndices

  ├─ FNetDependencyData SubObjects // subobject·child-subobject·dependent-parent index arrays 관계도 제공
  ├─ SubObjectInternalIndices / DependentObjectInternalIndices
  ├─ DependentObjectsWithParentFilter
  └─ ObjectsWithDependentObjectsInternalIndices

FReplicationWriter / Reader / Bridge
└─ InternalIndex를 통해 동일 object record 접근

---
---
---

UReplicationSystem::NetUpdate
├─ UpdateDirtyObjectList
├─ UpdateWorldLocations

├─ UpdateFiltering

├─ UReplicationBridge::CallPreSendUpdate ──> Fragment poll
├─ UpdateDirtyListPostPoll 

   UpdateConditionals

├─ QuantizeDirtyStateData

├─ UpdateObjectScopes
   PropagateDirtyChanges

├─ UpdatePrioritization
   baseline preparation // optional

---
---
---

UNetDriver::TickDispatch
UNetDriver::InternalTickDispatch
{
  this::ReplicationSystem::PreReceiveUpdate();

  // socket → PacketHandler::Incoming → UNetConnection::ReceivedPacket → bunch 분배

  UDataStreamChannel*::ReceivedBunch
  {
    UDataStreamManager*::ReadData(Context);
      // → UReplicationDataStream → Reader:FReplicationReader*::Read():
      //    · 상태: dequantize → FReplicationInstanceProtocol 로 UObject property 적용, RepNotify
      //    · attachment(RPC): NetRPCHandler → 로컬 UFunction 실행
      //    · 미해결 참조: ObjectReferenceCache 가 로드 후 지연 적용
  }

  this::ReplicationSystem::PostReceiveUpdate();
}

---
---
---

RPC — attachment 로의 일반화
대상 Net Object에 붙는 Net Blob attachment

UObject + INetBlobReceiver
└─ UNetBlobHandler
   └─ UNetRPCHandler

FNetBlob
└─ FNetObjectAttachment
   └─ FNetRPC

AActor::CallRemoteFunction()
{
  (Iris 분기)
  UReplicationSystem*::SendRPC()
  {
    // FNetRPC(NetBlob) 생성 — "객체에 붙는 attachment"
    // 대상 객체의 attachment queue가 관리
  }
}

---
---
---

UReplicationBridge
  UObjectReplicationBridge
    UEngineReplicationBridge

---

FReplicationConnections 
  FReplicationConnection
    FReplicationWriter
    FReplicationReader
    UDataStreamManager


UDataStreamChannel
  UDataStreamManager
    

---

UNetDriver::TickFlush
  UpdateIrisReplicationViews
  SendClientMoveAdjustments
  UReplicationSystem::NetUpdate

  per-connection
  UNetConnection::Tick
  UDataStreamChannel::Tick

  ReplicationSystem::PostSendUpdate()

UNetDriver::TickFlush
  ServerReplicateActors
    ActorChannel/Replicator

---
---
---

FRepLayout 

스키마    : Cmds:FRepLayoutCmd[]            // property 순회·직렬화 명령 리스트
비교      : CompareProperties()             // shadow 대비 changelist 생성
직렬화    : SendProperties()/ReceiveProperties()
RPC       : SendPropertiesForRPC()/ReceivePropertiesForRPC

mutable 값
├─ 객체 공통: FRepChangelistState
└─ 연결별  : FRepState

---

FReplicationInstanceProtocol
  FReplicationFragment
  FFragmentData
    ExternalSrcBuffer
  EReplicationInstanceProtocolTraits
  FragmentCount

---
---
---
---
---

UReplicationBridge
├─ UReplicationSystem*

├─ FReplicationProtocolManager*
├─ FReplicationStateDescriptorRegistry*
├─ FNetRefHandleManager*
├─ FObjectReferenceCache*

├─ FNetObjectGroups*
├─ TMap<FObjectKey, FNetObjectGroupHandle> LevelGroups

├─ TMap<FNetRefHandle, EEndReplicationFlags> HandlesToStopReplicating
├─ TMap<FNetRefHandle, FDestructionInfo> StaticObjectsPendingDestroy
└─ TArray<FPendingEndReplicationInfo> HandlesPendingEndReplication

---

UObjectReplicationBridge
  StartReplicatingRootObject

UEngineReplicationBridge
  StartReplicating / StopReplicating Actor / Component / SubObject

---
---
---

FNetRefHandleManager
├─ FRefHandleMap RefHandleToInternalIndex
├─ FNetHandleMap NetHandleToInternalIndex

├─ FNetBitArray CurrentFrameScopableInternalIndices
└─ FNetBitArray PrevFrameScopableInternalIndices
├─ FNetBitArray GlobalScopableInternalIndices / RelevantObjectsInternalIndices
├─ FNetBitArray PolledObjectsInternalIndices / DirtyObjectsToQuantize
└─ FNetBitArray AssignedInternalIndices / ObjectsWithPreUpdate
├─ FNetBitArray SubObjectInternalIndices / DependentObjectInternalIndices
├─ FNetBitArray DependentObjectsWithParentFilter
├─ FNetBitArray ObjectsWithDependentObjectsInternalIndices
├─ FNetBitArray DestroyedStartupObjectInternalIndices
└─ FNetBitArray WantToBeDormantInternalIndices / DormantObjectsPendingFlushNet

---
---
---

FObjectReferenceCache

TMap<const UObject*, FNetRefHandle>
TMap<FNetRefHandle, FCachedNetObjectReference>
TMap<FNetRefHandle, FQueuedBatchObjectReference>
TMultiMap<FNetRefHandle, FNetRefHandle>

FNetObjectReference
├─ FNetRefHandle RefHandle
├─ FNetToken PathToken
└─ ENetObjectReferenceTraits Traits

---
---
---

FReplicationWriter (로컬 → 원격 송신 상태 기계)
  FReplicationParameters Parameters
  FReplicationRecord ReplicationRecord

  TArray<FReplicationInfo> ReplicatedObjects // 객체별 송신 상태

FReplicationInfo 
├─ FChangeMaskStorageOrPointer
├─ ChangeMaskBitCount / State / HasDirtySubObjects / IsSubObject
├─ HasDirtyChangeMask / HasAttachments / HasChangemaskFilter
├─ IsDestructionInfo / IsCreationConfirmed / TearOff
├─ SubObjectPendingDestroy / IsDeltaCompressionEnabled
├─ LastAckedBaselineIndex / PendingBaselineIndex / FlushFlags
└─ HasDirtyConditionals / HasCannotSendInfo

---

FReplicationWriter
  cached subsystem pointers
    FReplicationSystemInternal
    FNetRefHandleManager
    UReplicationBridge
    FDeltaCompressionBaselineManager
    FObjectReferenceCache
    FReplicationFiltering
    FReplicationConditionals
    UPartialNetObjectAttachmentHandler
    UNetObjectBlobHandler
    FNetExports
    FNetTypeStats

---

FReplicationReader (원격 → 로컬)
  TMap<uint32, FReplicatedObjectInfo> ReplicatedObjects

FReplicatedObjectInfo
  UnresolvedChangeMaskOrPointer
  UnresolvedObjectReferences / ResolvedDynamicObjectReferences
  UnresolvedHandleCount / ResolvedDynamicHandleCount
  uint8* StoredBaselines[2] / uint32 InternalIndex
  ChangeMaskBitCount / bHasUnresolvedReferences
  bHasUnresolvedInitialReferences / bHasAttachments
  bDestroy / bTearOff / bIsDeltaCompressionEnabled
  LastStoredBaselineIndex / PrevStoredBaselineIndex / Padding

---

FGlobalDirtyNetObjectTracker → FDirtyNetObjectTracker
→ Bridge(poll·quantize) → FReplicationStateStorage
→ FReplicationFiltering(Groups·WorldLocations) → FReplicationConditionals → FReplicationPrioritization
→ (연결별) FReplicationWriter(+BaselineManager, NetBlobManager, ObjectReferenceCache 의 export)
→ UReplicationDataStream → UDataStreamChannel → bunch/packet

---
---
---

UObject 를 만지는 순간은 복사해 올 때와 수신측에서 되돌려 쓸 때뿐

UEngineReplicationBridge::StartReplicatingActor
  Fragment 생성·등록

Poll: UObject → External State
Apply: External State → UObject / RepNotify

UObject 접근을 줄인 내부 경로
Internal State → Filtering/Prioritization/Writer/Baseline

---
---
---

external representation : C++ 멤버 선언 그대로의 값 형태 

internal representation : serializer-defined quantized layout

replication state       : 함께 관리되는 프로퍼티들의 묶음 1벌. 한 클래스는 여러 state 를 가질 수 있다

changemask              : state 안에서 "어느 member 가 변했나"를 나타내는 Descriptor가 정한 비트 지

---
---
---

// 클래스당 1회 호출되는 정적 등록
void AMyCharacter::GetLifetimeReplicatedProps(TArray<FLifetimeProperty>& OutProps) const
{
  FDoRepLifetimeParams Params;                             
  Params.bIsPushBased = true;                              // push model 선언

  DOREPLIFETIME_WITH_PARAMS_FAST(AMyCharacter, Health, Params);
}

FLifetimeProperty
├─ RepIndex
├─ RepNotifyCondition
└─ bIsPushBased

RepIndex

---
---
---

Iris 초기화 시 FReplicationStateDescriptorBuilder 가 UClass 를 순회하며 위 선언을 읽고, 
복제 프로퍼티들을 몇 벌의 state 로 묶어 state 마다 descriptor 를 하나씩 굽는다

FReplicationStateDescriptorBuilder
  CreateDescriptorsForClass
  CreateDescriptorForStruct
  CreateDescriptorForFunction

---
---
---


MARK_PROPERTY_DIRTY_FROM_NAME(AMyActor, Health, this)
UEPushModelPrivate::MarkPropertyDirty(this, PrivatePushId, GET_PROPERTY_REP_INDEX(AMyActor, Health))

PushId:FNetPushObjectId = Object->GetNetPushIdDynamic() // 객체에 심어진 '통지용 주소'
//   uint64 하나에 두 백엔드를 인코딩: 상위 32bit 가 전부 1이면 고전 스택용 id
//   아니면 Iris 용 (FNetHandle 기반)

↓ Iris 쪽이면, 등록된 delegate 로

FNetHandleLegacyPushModelHelper::MarkPropertyOwnerDirty(Object, PushId, RepIndex)
{
  ::MarkNetObjectStateDirty(Handle.GetNetHandle());
}

---

Dirty macro

“이 object를 확인하라”는 scheduling signal
“그 object를 poll 필요 대상으로 표시”하는 신호

실제 값 비교와 member mask는 poll 때
internal 변환은 quantize 때
Connection별 전송은 Writer 때

---

FGlobalDirtyNetObjectTracker (프로세스 전역 수신함, NetCore 모듈)
  // dirty 객체의 FNetHandle 집합을 프레임 동안 누적.
  // 복제 시스템이 여럿(게임·리플레이 녹화)이라 소비자(poller)도 여럿 

↓ 프레임 송신 시작 시, 시스템마다

FDirtyNetObjectTracker::UpdateDirtyNetObjects()
  // 전역 집합의 FNetHandle 을 자기 내부 index 로 번역
  // DirtyNetObjects:FNetBitArray 에 비트 세팅

  ↓

FDirtyNetObjectTracker::ReconcilePolledList(ObjectsPolled)
  // 이번에 실제 방문한 객체만 클리어 — 예산상 못 간 객체는
  // AccumulatedDirtyNetObjects 에 남아 다음 프레임으로 이월

---
---
---

타입                  발급자                    통용 범위        wire     지칭 대상
FNetHandle            FNetHandleManager         프로세스 전역     ✗       
FNetRefHandle         서버 ReplicationSystem    양 끝단           ✓       
FInternalNetRefIndex  FNetRefHandleManager      한 시스템 내부    ✗       장부의 배열 좌표

FNetPushObjectId      PushModel 계층            매크로↔백엔드     ✗       
FNetObjectReference                             직렬화 값         ✓       
FNetToken             FNetTokenStore            연결 간 사전      ✓       문자열의 사전 번호

---

프로세스 (NetCore)
  FNetHandle { Id:uint32, Epoch:uint32 }
  · 발급: FNetHandleManager. UObject 하나당 하나, 앱 수명 동안 유일
  · 통용: 프로세스 전역 — 다수 복제 시스템들 공용
  · wire: ✗

복제 세계 (Iris, 양 끝단 공유)
  FNetRefHandle { Id:60bit, ReplicationSystemId:4bit }
  · 발급: 서버(권위측)의 ReplicationSystem
  · 통용: 서버·클라이언트가 공유
  · 홀짝 규약: static 객체(레벨 배치물·에셋 — 이름으로 찾을 수 있는 것)는 홀수 Id,
    dynamic 객체(런타임 spawn — 만들어야 하는 것 / 서버권위)는 짝수 Id.
    Id 의 홀짝이 곧 수신측의 resolve 방법을 말해준다

한 시스템의 작업장 좌표 (Iris 내부)
  FInternalNetRefIndex = uint32 (typedef)
  · 발급: 그 ReplicationSystem 의 FNetRefHandleManager
  · 통용: 그 시스템 내부
  · 역할: 조밀한 배열 index — handle 로 map 조회는 입구에서 한 번, 이후는 index 

---

FNetTokenDataStore
  FStringTokenStore
  FNameTokenStore
    FGameplayTagTokenStore

---

FNetTokenStore
TArray<TTuple<FName, TUniquePtr<FNetTokenDataStore>>> TokenDataStores

FNetToken
Index 20-bit / TypeId 3-bit / bIsAssignedByAuthority 1-bit / Padding 8-bit

---

FNetObjectReference
├─ RefHandle ──> 동적/replicated object identity
├─ PathToken ──> stable named object path

대상이 복제 객체면 RefHandle 로
아니면(에셋 등) 경로 기반으로

FObjectReferenceCache
└─ create / export / read / resolve

---
---
---

FReplicationSystemUtil::BeginReplication
  UEngineReplicationBridge::StartReplicatingActor
    UObjectReplicationBridge::StartReplicatingRootObject
      UObjectReplicationBridge::StartReplicatingNetObject
        1 FObjectReferenceCache::CreateObjectReferenceHandle
        2 CallRegisterReplicationFragments
            FReplicationProtocolManager::CreateInstanceProtocol
        3 FNetHandleManager::GetOrCreateNetHandle
        ...

---
---
---

[서버 — 값 저장소]

V1  AMyCharacter::Health:float                  // 원본. 게임 스레드 소유

V2  external state buffer 의 member 슬롯        // FPropertyReplicationState 가 관리

V3  internal state buffer 의 member 슬롯        

Writer A       Writer B          Writer C

V4  baseline                                   // 각 Connection의 '상대가 ack 한 시점'의 quantized 사본
                                             
---
---
---

[서버 — 신호 저장소 (값이 아니라 '어디가 변했나')]
S1  FGlobalDirtyNetObjectTracker 의 dirty 집합                // FNetHandle 
S2  FDirtyNetObjectTracker::DirtyNetObjects:FNetBitArray      // 시스템 내부 index 

S3  V2 안의 FReplicationStateHeader dirty + changemask 비트   // state·member
S4  FChangeMaskCache                                          // 이번 프레임에 확정된 마스크들의 수집처

S5  FReplicationWriter::ReplicatedObjects:FReplicationInfo[] 의 pending mask   // 연결별 미전송분, ChangeMaskOrPtr
S6  FReplicationRecord                                                         // packet 별 '무엇을 실었나'

---
---
---

[클라이언트 — 값 저장소]
V5  FReplicatedObjectData::ReceiveStateBuffer:uint8*   // FNetRefHandleManager에 매달린
                                                       // 수신측 quantized 사본 — 델타 해독의 기준

V6  external state buffer (수신측)               // dequantize 결과

---

wire bitstream
  FReplicationReader deserialize/delta

V5 Receive Internal State / baseline
  FNetSerializer::Dequantize

V6 External State (dispatch용 임시 또는 fragment buffer)
  FReplicationFragment::ApplyReplicatedState

V7 UObject native values
  RepNotify / attachment-RPC

---
---
---

① 변경 + 통지 (게임 코드, 아무 때나)
   Health = 75.f;                                   // V1 갱신
   MARK_PROPERTY_DIRTY_FROM_NAME(…);                // → S1 에 FNetHandle 추가. 값 이동 없음
   사본 현황:  V1=75 │ V2=옛값 │ V3=옛값(q) │ V4=옛값(q)

② 송신 프레임 시작
   FDirtyNetObjectTracker::UpdateDirtyNetObjects(); // S1(FNetHandle) → S2(internal index 비트)
   // 이 시점까지 시스템은 "그 객체가 변했다"만 안다

③ 수확(poll) — dirty 객체만 방문
   FPropertyReplicationFragment::PollReplicatedState()
     FPropertyReplicationState::PollPropertyReplicationState(&V1);
     // V1 을 V2 와 member 별 비교 → Health 만 다름 
     // changemask 의 Health 비트 세팅 (S3)
   사본 현황:  V1=75 │ V2=75 │ V3=옛값(q) │ V4=옛값(q)

---

dirty macro가 member bit를 직접 켜지 않는다
Push Model에서는 object를 dirty로 표시하고 이후 poll 비교가 실제 member change mask를 만든다

---

④ 양자화
   FReplicationInstanceOperationsInternal::QuantizeObjectStateData();
   // V2 의 변한 member 만 serializer 로 quantize 해 V3 에 기록,
   // 확정된 마스크는 S4(FChangeMaskCache)로
   // 이후 모든 비교·전송은 이 형태 기준
   사본 현황:  V1=75 │ V2=75 │ V3=75(q) │ V4=옛값(q)

⑤ 연결 분배 — 값이 아니라 신호만 갈라진다
   Writer(연결당 1개)가 S4 의 마스크를 자기 S5(pending mask)에 OR
   // 연결이 100개여도 75 라는 값의 사본은 늘지 않는다. 마스크만 100벌

---

⑥ 직렬화 — 연결별 델타
   FReplicationWriter::Write
   // V3(현재) 를 V4(이 연결의 baseline) 대비 SerializeDelta → 비트열.
   // "75" 가 아니라 "옛값에서 75 로의 차분"이 나간다
   // S5 의 마스크를 S6(FReplicationRecord)에 (PacketId 와 함께) 기록
   // pending → in-flight 로 이전 — "실어 보냈고 결과 대기 중"
   여기서 값의 형태:  quantized 값 → 델타 비트열 (연결마다 다를 수 있음)

⑦ 전송 접점
   UReplicationDataStream → UDataStreamChannel 의 bunch → packet
   // 이하는 전송 계층(seq/ack, 암호화·압축)

---

Connection Writer
   ├─ accumulated mask
   ├─ optional acked baseline delta
   ├─ references/attachments/exports
   └─ replication record 생성

object별 Delta Compression 허용
유효 baseline
connection scope
delivery notification

---
---
---

════════ wire: 델타 비트들이 packet payload 로 이동 ════════

⑧ 해독 (클라이언트)
   UDataStreamChannel::ReceivedBunch → … → FReplicationReader::Read
   // changemask 를 먼저 읽고 → 마스크가 가리키는 member 만
   // V5(ReceiveStateBuffer, 수신측 baseline) 대비 델타 해독 → V5 를 75(q) 로 갱신
   사본 현황(클라):  V5=75(q) │ V6=옛값 │ V7=옛값

⑨ 적용
   FPropertyReplicationFragment::ApplyReplicatedState()
     // dequantize: V5 의 quantized 값 → external 형식 → V6
     FPropertyReplicationState::PushPropertyReplicationState(Owner, &V7);
     // V6 → V7 (UObject 멤버에 기록)
     FPropertyReplicationState::CallRepNotifies(…);
     // 이전 값과 비교해 OnRep_Health 호출 
     사본 현황(클라):  V5=75(q) │ V6=75 │ V7=75 → gameplay 반응

---

⑩ [서버] ack/nak 
   ack  → FReplicationWriter::HandleDeliveredRecord()
          // S6 의 in-flight 해소. V4 baseline 을 75(q) 로 갱신
          // "상대가 아는 과거"가 전진
   nak  → FReplicationWriter::HandleDroppedRecord()
          // 잃어버린 packet 에 실렸던 마스크를 S5(pending)로 되돌림.
          // 다음 기회에 '그때의 최신 V3' 로 다시 직렬화
          // 보냈던 비트의 재전송이 아니라 상태로부터의 재파생

---
---
---

Reflection metadata
UPROPERTY(Replicated/ReplicatedUsing)
  → FProperty::PropertyFlags에 CPF_Net
  → FProperty::RepIndex
  → ReplicatedUsing이면 FProperty::RepNotifyFunc


lifetime metadata
GetLifetimeReplicatedProps()
  → FLifetimeProperty[]
      RepIndex:uint16
      bIsPushBased:bool
      RepNotifyCondition:ELifetimeRepNotifyCondition


FReplicationStateDescriptorBuilder
  └─ FProperty + FLifetimeProperty를 Iris Layout으로 변환

---
---
---

타입의 모든 인스턴스가 공유
  FReplicationProtocol
    FReplicationStateDescriptor*[]
    protocol traits

Instance 1 ──┐
Instance 2 ──┼─> 같은 compatible Protocol 참조
Instance N ──┘

---

객체 인스턴스마다 존재
  FReplicationInstanceProtocol
    FPropertyReplicationFragment
      FPropertyReplicationState
    FFragmentData
      ExternalSrcBuffer


FReplicatedObjectData
  Protocol / InstanceProtocol
  ReceiveStateBuffer


FNetRefHandleManager
└─ ReplicatedObjectStateBuffers[InternalIndex]

---
---
---

FReplicationStateDescriptor
 
복제 타입 전체보다 작은 단위인 Replication State 하나
한 UClass가 여러 State Descriptor를 가질 수 있다

FReplicationStateMemberDescriptor
FProperty** MemberProperties
FReplicationStateMemberPropertyDescriptor

FReplicationStateMemberChangeMaskDescriptor
FReplicationStateMemberSerializerDescriptor

FReplicationStateMemberLifetimeConditionDescriptor
FReplicationStateMemberTraitsDescriptor
FReplicationStateMemberFunctionDescriptor
FReplicationStateMemberTagDescriptor
FReplicationStateMemberReferenceDescriptor

EReplicationStateTraits

---

FReplicationProtocol
  FReplicationStateDescriptor*[]
  FReplicationProtocolIdentifier
  EReplicationProtocolTraits

---
---
---

FReplicationInstanceProtocol
  FReplicationFragment*[]
  FFragmentData[]
  EReplicationInstanceProtocolTraits

---

FReplicationFragment
└─ FPropertyReplicationFragment
    Owner:UObject*
    SrcReplicationState: FPropertyReplicationState
    PrevReplicationState: FPropertyReplicationState
    FReplicationStateDescriptor*
    
    PollReplicatedState
    ApplyReplicatedState

---
---
---

external(source, 게임 타입)  ↔  internal(quantized, 네트워크 타입)
member 하나가 오프셋을 둘 갖는다
두 offset 표는 같은 member의 서로 다른 representation을 가리킨다

FReplicationStateDescriptor
  FReplicationStateMemberDescriptor[i] = { ExternalMemberOffset, InternalMemberOffset }
  
---

builder는 요구에 따라 state를 분할

일반 프로퍼티                 70개 → Regular State Descriptor 1개
COND_InitialOnly              10개 → InitOnly State Descriptor 1개
Lifetime Conditional          15개 → Conditional State Descriptor 1개
Custom Replication Fragment    5개 → Custom Descriptor 5개

---
---
---

Gameplay World

Shared Replication World
  NetRefHandle로 등록된 Object
  polled external state, quantized internal state, global dirty/change mask와 protocol
  목적: 이번 Update에서 무엇이 변했는지 공통으로 표현


Per-Connection World
  Connection별 전달 상태
  목적: 이 Client가 무엇을 이미 알고 있는지 표현
  change mask, priority, delivery record, baseline

---

UWorld::BroadcastTickFlush
  UNetDriver::TickFlush
   ReplicationSystem->NetUpdate
     StartPreSendUpdate
     Dirty / WorldLocation / Filtering update
     UReplicationBridge::CallPreSendUpdate
       poll list를 Fragment가 poll
     Post-poll dirty / conditionals
     QuantizeDirtyStateData
     ProcessNetObjectAttachmentSendQueue
     UpdateObjectScopes / PropagateDirtyChanges
     ResetNetObjectAttachmentSendQueue
     UpdatePrioritization
     FDeltaCompressionBaselineManager::PreSendUpdate

---
---
---

FReplicationState

같은 high-level 조건과 같은 owner를 공유하는 프로퍼티 묶음
그래서 오브젝트 하나가 여러 state로 나눠질 수 있다

"State"는 값 스냅샷

FReplicationStateDescriptor  =  그 state의 schema
state buffer (uint8*)        =  그 state의 instance

State는 "이 묶음 프로퍼티들의 현재 값"

quantize로 포착하고, IsEqual/delta로 비교하고, dequantize+apply로 재현하는 대상이 State
Descriptor는 그 State 버퍼의 레이아웃을 적은 것

---
                     
UObject/native data source
        ▲             │
        │ Apply       │ Poll
        │             ▼
FReplicationFragment

---

FFragmentRegistrationContext
FReplicationFragmentInfo
FReplicationFragment
  행동 인터페이스
  
---
---
---

quantized 결과를 모든 커넥션이 공유
오브젝트당 한 번 quantize → N개 커넥션이 재사용
커넥션마다 게임 오브젝트를 다시 안 건드리는 이유

Current Quantized Internal State Q
   ├─ Writer A
   │  ├─ Mask A
   │  ├─ Baseline A
   │  └─ payload A
   ├─ Writer B
      ├─ Mask B
      ├─ Baseline B
      └─ payload B

---

external 저장소

FPropertyReplicationState
  uint8* StateBuffer
  FReplicationStateDescriptor*

FPropertyReplicationFragment
  SrcReplicationState
  optional PrevReplicationState
  ReplicationStateDescriptor*
  UObject* Owner

FReplicationInstanceProtocol
└─ FragmentData[i].ExternalSrcBuffer ──> 위 buffer

---

게임 타입이 들어오니 memcpy로 못 만들고 진짜 ctor/dtor가 필요 
그래서 descriptor가 ConstructReplicationState / DestructReplicationState 함수 포인터를 든다. 
타입이 POD면 IsSourceTriviallyConstructible / IsSourceTriviallyDestructible trait로 그 호출을 생략

---

FPropertyReplicationState

생성 방식에 따라 buffer를 소유할 수도 있고, 
외부에서 제공한 external buffer를 non-owning 방식으로 감쌀 수도 있다

---
---
---

internal 저장소

[Net Object local current send state]
FNetRefHandleManager::ReplicatedObjectStateBuffers

[Net Object remote current receive state]
FReplicatedObjectData::ReceiveStateBuffer

[Replication System send/receive storage]
FReplicationStateStorage::FPerObjectInfo

---

서버·클라이언트 역할과 복제 방향에 따라 send/receive storage 에 대한 필요가 다르다

---

FPerObjectInfo
├─ Protocol:FReplicationProtocol*
├─ StateBuffers[SendState, RecvState]:uint8*   // 각 크기 = Protocol::InternalTotalSize
└─ AllocationCount

---

불투명 byte 버퍼
타입 정보는 버퍼에 없고 전부 descriptor/serializer가 해석

dynamic state는 포인터로
array 같은 컨테이너는 버퍼 상단엔 작은 헤더(개수+포인터)만 POD로 두고 실제 원소는 힙에 quantized로

---

네트워크 표현
object ref는 raw 포인터가 아니라 FNetRefHandle/NetObjectReference

current + history
SendState/RecvState(현재)뿐 아니라 acked baseline들을 함께 저장해 delta 기준을 유지

---

FReplicationStateOperations
FReplicationStateOperationsInternal
FReplicationProtocolOperationsInternal

---
---
---

UObject* / TWeakObjectPtr
└─ FObjectNetSerializer / FWeakObjectNetSerializer
   └─ FNetObjectReference(handle/path token/traits)

FName
├─ FNameNetSerializer
└─ FNameAsNetTokenNetSerializer
   └─ FNameAsNetTokenNetSerializerQuantizedType

FString
└─ FStringNetSerializer

TArray<Element>
└─ FArrayPropertyNetSerializer

---

정준화
Canonicalization

서브정밀도 노이즈를 같은 버킷으로 뭉개서 dirty 오탐 방지
0.001° 차이 나는 두 원본이 같은 short로 압축되면 안 dirty → 안 보냄
원본 float를 그냥 비교했다면 매 프레임 미세 노이즈로 재전송했을 것

external 값을 직접 IsEqual
두 값을 먼저 Quantize 후 IsEqual

---

FVectorNetQuantizeNetSerializerBase<InConfigType, InScaleBitCount>
  ├─ FVectorNetQuantizeNetSerializer        <FVectorNetQuantizeNetSerializerConfig, 0U>
  ├─ FVectorNetQuantize10NetSerializer      <FVectorNetQuantize10NetSerializerConfig, 3U>
  └─ FVectorNetQuantize100NetSerializer     <FVectorNetQuantize100NetSerializerConfig, 7U>
  
---
---
---

액터마다 × 커넥션마다 UActorChannel, FObjectReplicator
N액터 × C커넥션 = N×C개 채널 객체

IRIS 대응: 액터당 채널 대신 커넥션당 DataStream 하나
오브젝트는 그 스트림 안 batch로

---

hot loop가 액터→컴포넌트→UProperty 링크드리스트를 걷고
IsNetRelevantFor·GetNetPriority 같은 virtual 호출을 액터×커넥션마다 해
포인터 체이싱 → 캐시 미스 폭발

---

FRepLayout::CompareProperties가 shadow와 비교해 changelist(뭐가 바뀌었나)를 만드는데, 
이건 FReplicationChangelistMgr로 액터당 1회 계산해 커넥션들이 공유

---

FNetRefHandleManager
  ReplicatedObjectData::ReceiveStateBuffer
  ReplicatedObjectStateBuffers 

둘 다 uint8* internal(quantized) 버퍼고 크기도 같아
FReplicationProtocol::InternalTotalSize

차이는 송신/수신 방향

---

FNetRefHandleManager::ReplicatedObjectStateBuffers:TNetChunkedArray<uint8*>[i]

송신(outgoing) current 상태
quantize가 여기에 쓰고(Impl::QuantizeDirtyStateData()), writer가 여기서 읽어 delta 직렬화

---

FNetRefHandleManager::ReplicatedObjectData:TNetChunkedArray<FReplicatedObjectData>[i]::ReceiveStateBuffer:uint8* 

수신(incoming) current 상태
reader가 deserialize해 여기에 넣고, dequantize→apply

TODO: 사용 패턴별로 배열 분리 주석

---

송신 버퍼는 매 프레임 전체를 index로 훑는 hot 루프
포인터만 담은 dense 컬럼으로 빼면 캐시 친화적

수신 버퍼는 apply 때 그 오브젝트 하나만 만지니 레코드 안에 둬도 충분

---

FReplicationStateStorage 

Replication Internal의 서브시스템

baseline(과거 스냅샷) 소유
현재 상태는 GetState(CurrentSend)로 위임

---

Buffer 블록 하나 = 시점 하나
Storage 저장고 = 과거를 함축

---

UDataStreamChannel
  UDataStreamManager: UDataStream
  비트스트림 버퍼(2048B) — 조립 중 패킷

---

UReplicationDataStream

한쪽: 범용 DataStream 프레임워크
다른쪽: IRIS 복제 로직(FReplicationWriter/Reader)

UReplicationDataStream::WriteData(Context) → ReplicationWriter->Write(Context)
UReplicationDataStream::ReadData(Context)  → ReplicationReader->Read(Context)

---
---
---
---
---

Descriptor (per-class)
  HOW = MemberProperties[](FProperty→UObject offset)

Fragment   (per-instance)
  WHICH = Owner:UObject*
  Descriptor ref + ExternalBuffer

---
---
---
