"Destruction, Cloth, Fluid, Rigid Body를 하나의 통합 솔버 위에서 돌린다"

Game Thread                    Physics Thread
─────────────────────         ─────────────────────
UWorld                         FPhysScene_Chaos
UPrimitiveComponent     ←→     FBodyInstance / FActorHandle
FBodyInstance (proxy)          Chaos::FRigidBodyHandle
AActor transform               Chaos particle state

Physics 시뮬레이션은 결정론적 고정 timestep으로 돌아야 하고
Game Thread는 가변 프레임레이트로 돌아

게임 레이어
UPrimitiveComponent가 물리의 진입점이야. 
UStaticMeshComponent, USkeletalMeshComponent 등이 여기 속해
FBodyInstance를 멤버로 갖고 있는데, 
이게 Game Thread 쪽의 물리 상태 프록시야.

FBodyInstance
Game Thread 코드가 물리 상태를 읽고 쓸 수 있는 인터페이스를 제공하면서,
동시에 Physics Thread의 실제 파티클 핸들을 감싸는 래퍼 역할

Scene 레이어
UWorld는 FPhysScene을 소유 (구현체는 FPhysScene_Chaos)
이게 물리 월드의 컨테이너
FPhysScene이 Physics Thread를 실제로 구동하고 시뮬레이션 tick을 조율

Chaos Solver 레이어
PBD (Position Based Dynamics) 방식으로 constraint를 푼다
PhysX (impulse-based)와의 주요 차이

PBD는 위치를 직접 보정하는 방식이라 안정성이 높고 대규모 rigid body에 유리
대신 에너지 보존이 impulse 방식보다 덜 정확

Geometry / Collision 레이어
충돌 형상은 FKAggregateGeom으로 표현
(sphere, box, capsule, convex hull의 집합)
런타임에는 Chaos의 implicit geometry 표현으로 변환

Collision query (raycasting, sweep, overlap)는 
FPhysicsInterface를 통해 들어오고, 
결과는 FHitResult로 Game Thread에 반환

[Game Thread]
  AActor::Tick()
  → UPrimitiveComponent transform 변경
  → FBodyInstance에 변경 사항 기록 (dirty)

[Physics Thread Kick]
  FPhysScene::StartFrame()
  → dirty body들을 Physics Thread에 push (marshal)
  → Chaos solver tick (고정 substep, e.g. 1/60s)
  → 충돌 감지 → constraint resolution → 위치 적분

[Game Thread Sync]
  FPhysScene::EndFrame() / WaitPhysScenes()
  → Physics Thread 결과를 Game Thread로 pull back
  → FBodyInstance가 결과를 UPrimitiveComponent에 반영
  → AActor transform 업데이트

이 마샬링 구조 때문에 물리 결과가 1 프레임 지연되는 건 피할 수 없는 구조적 특성이야. 
이게 눈에 띄는 경우가 있어서 Async Physics 모드와 일반 모드를 선택적으로 쓸 수 있어.

Chaos Destruction (Geometry Collection)
Fracture 데이터를 미리 구워두고 (Voronoi 또는 수동 분할)
런타임에 조각들을 Chaos rigid body로 활성화
AGeometryCollectionActor와 UGeometryCollectionComponent가 진입점

Chaos Cloth
UChaosClothComponent가 관리
메시 버텍스를 파티클로 취급하고 constraint로 연결해서 천 시뮬레이션
물리 파이프라인 안에 통합되어 있어서 rigid body와 상호작용이 가능

Physical Animation
UPhysicalAnimationComponent가 애니메이션 포즈와 물리 시뮬레이션 결과를 블렌딩
FAnimInstanceProxy의 Physics blend 경로와 연결

URadialForceComponent
한 지점에서 방사형으로 impulse나 force를 쏘는 컴포넌트

Physics Fields
Anchor, Strain/Force, Sleep/Disable/Kill
공간에 규칙을 걸고, 그 공간에 들어온 파티클들이 그 규칙을 받게 하는 방식
대규모 파괴나 연출

Skeletal Mesh, Physics Asset
SkeletalMesh → PhysicsAsset rigid bodies/constraints → Chaos simulation
별도의 물리 신체를 만든 뒤, 그 결과를 다시 캐릭터 표현에 반영하는 과정
캐릭터 래그돌은 “본마다 물리 바디를 가진 Skeletal Mesh”라는 형태로 물리 세계에 투영되는 거예요

게임 코드에서는 SetSimulatePhysics, AddForce, SetCollisionProfileName 같은 API를 만지지만, 
실제론 이 호출들이 FBodyInstance를 통해 물리 씬으로 전달돼요

**“부딪히는지 검사하는 것”**과 
**“실제로 물리적으로 움직이게 하는 것”**을 분리

Query
- "여기서 앞으로 쏘면 무엇을 맞나?"
- "이 캡슐을 이 방향으로 밀면 어디에서 막히나?"
- 순간적인 검사

Simulation
- "이 통은 중력과 충격을 받아 실제로 굴러가나?"
- "이 문은 힌지 constraint로 흔들리나?"
- 시간에 따라 상태가 진화

Component / BodyInstance → PhysicsScene / Interface → Chaos Solver
그 연결을 담당하는 추상 경계가 FPhysScene 계열과 FPhysInterface_Chaos예요.
FBodyInstance::GetPhysicsScene()가 body의 소속 씬을 반환하고
FPhysInterface_Chaos는 Chaos를 호출하는 공용 인터페이스 역할을 해요

ExecuteWrite(FPhysScene*, ...) 같은 API가 따로 있는 것도, 
물리 상태를 임의의 타이밍에 마구 건드리지 않고, 
**적절한 쓰기 경로**를 통해 반영하려는 구조라는 신호예요

UPrimitiveComponent
   ↓
FBodyInstance
   ↓
Proxy (game thread ↔ physics thread bridge)
   ↓
Particle Handle
   ↓
Solver / Evolution
   ↓
충돌쌍 생성, 접촉 Solve, 
joint/constraint 처리, 속도/위치 갱신
sleep/wake 관리

cinematic-quality destruction을 
"실시간으로 다룰 수" 있다
Geometry Collection은 이 파괴 가능한 구조의 핵심 자산/표현

멀쩡한 벽
= 겉보기에는 하나

Chaos 내부
= 여러 조각들의 계층적 집합(cluster)

충격/strain 누적
→ 임계점 돌파
→ cluster 해제
→ 개별 조각이 독립 rigid body로 전환

UPhysicalMaterial
물체가 월드와 동적으로 상호작용할 때의 반응을 정의
friction, restitution, surface type 같은 차이

---

모든 것은 파티클이다
강체든, 천이든, 파괴된 조각이든
Chaos 솔버가 다루는 단위는 TGeometryParticle의 파생 타입

FGeometryParticle은 위치와 회전만 가진다
지형처럼 움직일 의도가 없는 정적 형상이 여기 해당

FKinematicGeometryParticle
속도 데이터를 가지지만 force에 반응하지 않는다
에스컬레이터처럼 애니메이션이 직접 움직이는 body

FPBDRigidParticle은 완전히 시뮬레이션되는 rigid body다. 
이게 "물리가 작동하는" body.

FPBDRigidParticle
고전역학에서 rigid body를 완전히 기술하는데 필요한 것들
위치 X, 회전 R, 선속도 V, 각속도 W, 질량 M, 관성 텐서 I와

FImplicitObject 
충돌 형상을 어떻게 표현하느냐
PhysX가 명시적 mesh를 저장하는 방식
Chaos는 implicit geometry
임의의 점 x에서 φ(x) = 부호 있는 거리를 반환하는 함수를 사용

FSphere, FBox, FCapsule, FConvex, FTriangleMeshImplicitObject, 
TImplicitObjectUnion (여러 shape 조합) 등이 파생된다.

---

조합 폭발이 발생하는 구조
O(n²) 충돌 쌍 검색

Broadphase
ISpatialAcceleration<FAccelerationStructureHandle> 인터페이스로 추상화
기본 구현은 TAABBTree

모든 파티클의 AABB를 계층적 트리로 관리
O(n log n)

TBoundingVolumeClusters는 공간적으로 근접한 sleeping body들을 그룹으로 묶는다. 
클러스터 전체가 다른 body와 overlap되지 않으면 내부는 아예 검사 안 해
sleeping body가 많은 large-scale destruction에서 핵심적인 최적화.

Narrowphase: GJK / EPA / SAT

Broadphase가 넘겨준 overlap 후보 쌍들 각각을 정밀하게 검사하는 단계.

GJK (Gilbert-Johnson-Keerthi)
두 convex shape 사이의 최소 거리
두 shape의 Minkowski difference가 원점을 포함하는지를 
iterative simplices 방법으로 검사

GJK가 penetration을 감지하면
EPA (Expanding Polytope Algorithm)로 전환해서 
침투 깊이와 접촉 법선을 계산한다.

Box-box에는 SAT (Separating Axis Theorem)가 별도 fast path로 존재
특정 primitive pair에 특화된 알고리즘이 더 빠르기 때문.

Solver & Constraints: 물리 법칙이 집행되는 곳
PhysX는 impulse-based 방식
충돌을 감지하면 운동량 보존에 따라 impulse를 계산해서 속도를 직접 수정
Chaos는 Position Based Dynamics (PBD)

PBD에서 constraint는 C(x) = 0 형태의 기하학적 조건
솔버는 이 조건을 만족시키도록 위치 x를 반복적으로 투영
속도는 위치 변화로부터 역산

impulse-based 방식은 수치 정밀도가 높지만 
constraint가 많아지면 (대규모 destruction처럼 수천 개의 접촉) 
시스템이 불안정해진다

PBD는 iteration 수를 줄이면 constraint가 덜 정확하게 풀리지만 
시스템이 발산하지 않고 안정하게 수렴한다

TPBDRigidsEvolution이 실제 적분 엔진
FPBDRigidsSolver가 외부 인터페이스를 제공

kinematic body들의 목표 위치를 적용
중력, RadialForce 등 외력을 모든 dynamic particle에 누적
symplectic Euler 적분으로 새 위치를 계산 (속도 먼저 업데이트, 업데이트된 속도로 위치 적분)
FContactPoint 목록을 생성해 FPBDCollisionConstraints에 등록
모든 constraint를 N회 반복해서 투영 (UPhysicsSettings에서 설정)
위치 변화에서 속도를 역산
에너지가 임계값 이하인 island를 sleeping으로 전환

Constraint 종류
FPBDCollisionConstraints가 contact manifold를 관리
restitution (반발계수), friction을 constraint coefficient로 인코딩. 
XPBD (Extended PBD)로 compliance 파라미터를 추가해서 restitution 튜닝 가능

FPBDJointConstraints가 물리적 조인트를 처리
각 joint는 6DOF (position 3 + rotation 3) constraint의 부분집합
Hinge joint는 rotation 2축을 잠그고 1축만 열어두는 식
FJointSolverGaussSeidel이 각 joint를 Gauss-Seidel iteration으로 풀어

FSimCallbackObject
솔버 루프 내 특정 실행 시점에 callback을 삽입하는 인터페이스
post-integrate, post-solve 같은 시점에 게임 코드가 개입
Chaos Vehicles의 타이어 마찰이 이 경로를 탄다
타이어 force는 매 physics substep마다 계산되어야 하고, 
GT 코드가 PT 루프 안에 정확한 타이밍으로 개입해야 하기 때문

Game Thread → Physics Thread 방향
FPhysCommandHandler (command queue) 
FSimCallbackObject::PhysicsCommands_External
비동기 command로 전달

Physics Thread → Game Thread 방향
매 프레임 끝에 FPhysScene_Chaos::EndFrame()에서 결과를 pull
FBodyInstance를 통해 UPrimitiveComponent로 반영

FPhysScene_Chaos는 FPBDRigidsSolver를 소유
프레임 경계에서 
StartFrame() → physics tick → WaitPhysScenes() / EndFrame() 구조로 GT와 동기화

FPhysCommandHandler
GT에서 오는 명령들을 lock-free queue로 받아서
physics tick 시작 전 일괄 처리

FPhysicsInterface 
모든 Physics Thread 작업을 static 함수로 추상화한 레이어

FPhysicsInterface::SetLinearVelocity_AssumesLocked(ActorHandle, Velocity);
FPhysicsInterface::AddImpulse_AssumesLocked(ActorHandle, Impulse);
FPhysicsInterface::SetMass_AssumesLocked(ActorHandle, Mass);

이 함수를 호출하는 코드는 이미 Physics Scene lock을 획득했다는 전제를 함수 이름에 인코딩

runtime assertion 대신 naming convention으로 전제를 명시화
type system 강제보다 약하지만 없는 것보다 낫다.

FBodyInstance
UPrimitiveComponent의 멤버
FPhysicsActorHandle ActorHandle
Physics Thread particle에 대한 handle

CollisionEnabled, ObjectType, CollisionResponses
Game Thread 쪽의 collision 설정을 담는다

FPhysicsInterface를 통해 PT에 particle을 생성

SetLinearVelocity() 같은 메서드는 내부에서 
FPhysicsCommand::ExecuteOnUnbrokenBody() 패턴으로 Scene lock 획득
→ FPhysicsInterface::*_AssumesLocked 호출의 흐름을 탄다

bSimulatePhysics
PT particle의 ObjectState를 Dynamic ↔ Kinematic 사이에서 전환하는 command가 
FPhysCommandHandler를 통해 PT에 전달

UPrimitiveComponent
렌더링 (SceneProxy), 
물리 (FBodyInstance), 
충돌 쿼리 (collision channel) 
세 시스템이 교차하는 지점

SetSimulatePhysics(true) → InitBody() → PT particle 생성의 체인이 
"물리 활성화"의 경로

AddForce(), AddImpulse() 등은 
FBodyInstance를 경유해서 PT로 전달

GetBodyInstance()로 FBodyInstance에 직접 접근할 수 있는데, 
이 경우 호출자가 Scene lock을 직접 관리해야 한다.

ECollisionChannel & ECollisionResponse
"각 CollisionChannel에 어떻게 반응하는가" (Ignore, Overlap, Block).

Block collision은 OnComponentHit을 발행
Overlap은 OnComponentBeginOverlap / EndOverlap을 발행

Chaos Destruction
UGeometryCollectionComponent
Fracture mesh를 미리 구워서
Voronoi 또는 수동 분할로 조각들을 생성
각 조각이 별도의 FPBDRigidParticle이 된다

FGeometryCollectionPhysicsProxy
GT ↔ PT bridge
FBodyInstance/FPhysicsInterface 경로 대신 destruction에 특화된 별도 proxy
수천 개의 조각이 clustered island로 관리

Sleeping cluster 메커니즘
연결된 조각들이 FPBDRigidClusteredParticle로 하나의 particle처럼 동작
멀쩡한 건물은 사실상 하나의 rigid body처럼 취급된다는 뜻

Chaos Cloth
UChaosClothComponent
메시 버텍스 → FPBDParticles 변환
Edge constraint (형태 유지), bending constraint (굽힘 저항), volume constraint (부피 보존) 등으로 
천의 물리적 특성을 Represent

FClothingSimulationChaos가 cloth simulation을 구동
rigid body collision과의 상호작용은 단순화된 capsule/sphere collision shape을 사용
Chaos main solver와 통합되어 있어서 
rigid body가 cloth에 반응하는 상호작용이 가능.

Physical Animation
UPhysicalAnimationComponent
Animation 시스템 (FAnimInstanceProxy)과 
Physics 시스템 (FBodyInstance)을 bridge

SetSkeletalBodySimulated()로 지정된 본부터 아래의 bone들을 
physics simulation으로 전환

애니메이션 포즈를 목표 값으로 삼아 
motor force를 물리 body에 적용

FPhysicalAnimationData의 OrientationStrengthMultiplier, AngularVelocityStrengthMultiplier
얼마나 강하게 포즈를 따라가는지 결정

FAnimNode_AnimDynamics
mass-spring proxy로 흔들림을 근사
Physical Animation은 FPBDRigidParticle을 사용하는 full Physics Thread simulation

---

Invariant Reading Point
함수는 함수 호출을 함축한다.
함수의 호출
  1. 고정된 life cycle 의 구조의 어느 시점에?
  2. 어떤 조건에서?

일종의 이벤트(사건)으로 보기
  고정 이벤트
  조건부 이벤트

---

Invariant Reading Point
함수는 구현자를 함축한다. 
  1. global scoped
  2. 파일 scoped
  3. 클래스 scoped -> object method
함수는 함수 호출을 함축한다.
함수의 호출은 호출자를 함축한다.
  1. 누가 이 코드블락을 소유하는가?
  2. 누가 이 코드블락을 호출하는가?

---

who allocoated this?
who ends this?
=>
who owns this?
=>
tree

---

what is its outermost context?
=>
tree root, 
thread context, 

---

what call protocol does this have?
=>
synchronous, asynchronous
lock free, lock assumed

---

모든 코드 구조물에는 특정 속성들이 항상 붙어있다
소유자가 있고, 호출자가 있고, 실행되는 컨텍스트가 있고, 실패 방식이 있다

코드가 반드시 함축하고 있는 속성들을 읽어내는 행위다

이 함수가 사용하는 리소스는 누가 소유하는가
이 함수가 생성하는 것은 누가 소유할 것인가

이 클래스는 무엇의 소유자인가
이 클래스 자신은 누구의 소유물인가

소유권이 없으면 유효성을 보장할 주체가 없어진다

FPhysicsInterface::SetLinearVelocity_AssumesLocked
이 코드는 Physics Scene lock이 이미 획득된 컨텍스트에서만 의미가 있다

실행 컨텍스트
스택 레이어
호출 스택의 어떤 위치에서 이것이 의미를 갖는가

순서 제약
FPhysScene_Chaos::StartFrame() 이전에 dirty body 마킹이 완료되어야 한다
InitBody() 이전에 SetCollisionProfileName()이 호출되어야 의미가 있다
의존성 제약의 다른 표현

선조건 (Precondition): 이것이 올바르게 작동하기 위해 사전에 참이어야 하는 것
후조건 (Postcondition): 실행 후 호출자에게 보장하는 것

---

금지된 것
불가능하게 된 것

FBodyInstance가 존재하는 이유
Game Thread 코드가 Physics Thread particle을 직접 건드리는 것을 불가능하게 만들기 위해

FPhysCommandHandler가 존재하는 이유
Game Thread가 임의 타이밍에 Physics Thread 상태를 변경하는 것을 금지하기 위해

이 구조물이 없다면 무엇이 가능해지는가?

---

시간적 비용 (복잡도, 지연)
공간적 비용 (메모리 레이아웃, 캐시 친화성)
부작용 비용 (상태 변경의 파급 반경)

---

어떤 조건에서 이 코드가 잘못 작동하는가?
실패가 발생할 때 그것은 즉각 드러나는가(loud), 아니면 조용히 잘못된 상태로 남는가(silent)
실패가 호출자(혹은 시스템)에 어떻게 전파되는가

---

그 존재는 진짜 실체인가, 프록시인가, 핸들인가?

누가 누구를 언제 움직이는가
state는 언제 누가 변경하고
명령은 어느 경로로 전달되는가

---

실제 물리 시뮬레이션의 살아 있는 상태는 Physics Thread 쪽 particle에 있고

UPrimitiveComponent.FBodyInstance는 Game Thread에서 그것을 읽고 쓰는 프록시

---

이것은 지속적으로 존재하는가, 한 프레임짜리 임시 값인가?
state

---

“A 세계 ↔ B 세계” 사이의 중간자들

이것은 중간자인가
아니면 독립자인가

---

시간의 선들

여러 선들의 합성으로 본다
즉, 각각의 선들을 독립적으로 떼어내서 본다
하나의 선을 식별한다

Game Thread
  Tick
  → dirty 기록

Physics Thread Kick
  → push / marshal
  → solver tick

Game Thread Sync
  → pull back
  → 결과 반영

---

발화 조건

함수를 인과계열의 결과로 본다
함수를 수신기로 본다

함수 자체에는 인과구조가 인코딩되어 있지 않다
하지만 전체는 인과구조를 인코딩한다

---
---
---
---
---
---
---