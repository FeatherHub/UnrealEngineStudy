게임 코드(UPROPERTY Replicated, RPC, RepNotify)는 최소 수정으로 호환
transport 계층(NetDriver, socket, PacketHandler)은 재사용
Iris가 교체하는 것은 "무엇을, 누구에게, 언제 보낼지 결정하고 직렬화하는" 중간 계층

---

Legacy의 비용 모델

net update마다 connection × actor를 순회하며
actor마다 가상함수(IsNetRelevantFor, GetNetPriority)

데이터 기반 일괄 처리로 대체


FRepLayout이 게임 오브젝트의 메모리를 shadow state와 직접 비교(polling)해서 변경을 감지

---

actor당 channel/bunch 구조를 없애고
connection당 소수의 stream

ReplicationDataStream: state 데이터
NetTokenDataStream:FName·object path 같은 반복 데이터를 작은 token으로 동기화

RPC는 오브젝트에 붙는 attachment(NetBlob)로 전송

---
---
---

PreSendUpdate
게임 데이터를 만지는 유일한 구간

legacy mode 오브젝트만 poll
dirty state를 Copy → Quantize → 내부 buffer 갱신, dirty reset
Filtering 갱신 → connection별 scope
connection별 Prioritization 갱신

---

SendUpdate
내부 데이터만 사용, connection별 병렬화 가능

connection마다 data stream을 tick하여 packet 구성

ReplicationDataStream:
      dirty object 스케줄 → priority·dependency 정렬
      → NetSerializer로 serialize (baseline 대비 delta)

NetDriver / socket (기존 transport)
 
---

packet 수신 → delivery notification(ack) 갱신
→ ReplicationDataStream:
    state 데이터 읽기
    새 오브젝트는 Bridge가 즉시 instantiate (protocol 구성에 필요)
    packet을 전부 읽은 뒤 dispatch:
      dequantize → 게임 오브젝트에 apply → RepNotify → RPC attachment 실행

---

Iris 전체가 UE_WITH_IRIS로 감싸져 legacy와 공존하는 구조가 
빌드 시스템 수준에서 강제됩니다

---

actor 생성 로직이 bridge에서 분리되어 NetActorFactory / NetSubObjectFactory(NetObjectFactoryRegistry에 등록)로 나왔습니다
수신 측 spawn을 factory 교체로 커스터마이즈할 수 있는 구조

---

Transport 접점은 Engine의 DataStreamChannel

---

IrisCore의 Private 디렉토리 구성 자체가 아키텍처 지도

ReplicationWriter/Reader, 
NetRefHandleManager,
DirtyNetObjectTracker, 
ObjectReferenceCache, 
ReplicationRecord, 
NetExports

Filtering
Prioritization
DeltaCompression
Polling
Conditionals
NetBlob
ChunkedDataStream

---

FRecordInfo
FReplicatedObjectInfo

---
---
---

FNetHandle
Net/Core
legacy와 Iris가 공유하는 인프라 계층

MARK_PROPERTY_DIRTY가 발생하는 순간에는 
이 오브젝트를 어떤 replication system이 복제 중인지 알 수 없습니다

그래서 dirty 통지는 시스템 중립적인 이 handle로 이루어지고
각 시스템의 dirty tracker가 자기 쪽 internal index로 번역합니다

---

FNetRefHandle
IrisCore
ReplicationSystem별 namespace

상대 peer와 "그 오브젝트"를 지칭할 때 쓰는 이름

---

FNetObjectReference

Property가 다른 UObject를 가리킬 때 wire에 실리는 형태
{RefHandle, PathToken(FNetToken), CanBeExported}

asset이나 레벨 지오메트리처럼 경로로만 지칭 가능한 대상은 PathToken으로
복제 중인 오브젝트는 RefHandle로

ObjectReferenceCache가 이 값을 만들고
수신측에서 해석하며, 즉시 해석이 안 되면 unresolved 추적으로 넘어갑니다

---

FReplicationConnection과 UserData

IrisCore에게 connection은 uint32 ConnectionId가 전부
UNetConnection이라는 타입은 코어의 의존성 그래프에 존재하지 않습니다

그런데 Iris 파이프라인에서 엔진 쪽 사실이 필요한 순간들이 있습니다. 
FReplicationConnection의 FObjectPtr UserData가 그 탈출구

SetConnectionUserData(ConnectionId, Connection)
UNetConnection 자신을 넣어둡니다

EngineReplicationBridge의 PIE 경로 remap
Cast<UNetConnection>(UserData)로 꺼내 GEngine->NetworkRemapPath에 넘깁니다

---

[로그인 경로 — ReplicationSystem 쪽]
NetDriver/NetConnection
  └ ReplicationSystem->AddConnection(ParentConnectionId)
      └ Connections[Id] 슬롯 생성
          Connection->ReplicationWriter = new FReplicationWriter
          Connection->ReplicationReader = new FReplicationReader
  └ SetConnectionUserData(Id, NetConnection)

[채널 경로 — Engine 쪽]
DataStream channel 생성 (정적 index, 양측 초기 채널)
  └ UDataStreamChannel::Init
      └ UDataStreamManager 생성
      └ ReplicationSystem->InitDataStreamManager(ParentConnectionId, Manager)
          └ 슬롯에 Manager 약참조 저장
          └ Manager가 ini 의 DataStreamDefinitions 대로 stream 생성
              (FInitParameters { ReplicationSystemId, ConnectionId, NetExports … } 전달)

[결선 — stream 자신이 pull]
UReplicationDataStream::Init(Params)
  └ GetReplicationSystem(Params.ReplicationSystemId)
      → Connections.GetConnection(Params.ConnectionId)
      → ReplicationWriter/Reader 포인터 복사, Writer에 NetExports 연결

---
---
---

FReplicationInfo 

connection당 Writer가 object마다 하나씩 갖는 상태
이 connection에 대해 이 object를 어떻게 보내야 하나

ChangeMaskOrPtr(보내야 할 변경의 누적분)

64bit 비트필드(현재 State 5bit, 
HasDirtyChangeMask, HasAttachments, IsDeltaCompressionEnabled, 
LastAckedBaselineIndex / PendingBaselineIndex, 
FlushFlags 등)

---

FReplicationRecord

connection당 하나 있는 in-flight 원장(ledger)

"아직 ack/nak가 안 온 packet들에 각각 무엇이 실렸나"를 보관
Packet 단위로 PushRecord/PopRecord 되고

object별로 FRecordInfoList로 꿰어져 있어 
"이 object의 in-flight 변경 전부"를 훑을 수 있습니다

---

FRecordInfo 

"packet 하나에 실린 object 하나"의 스냅샷
object index, 실었을 당시의 EReplicatedObjectState, 그 packet에 실린 changemask 사본(ChangeMaskOrPtr)...

---

object 하나를 packet에 직렬화한 직후 
CreateObjectRecord → CommitObjectRecord로 
위 스냅샷을 만들어 원장에 넣는 단계

---

Object batch에 state 값 외에 함께 실리는 세 부류
References / Attachments / Exports

References는 state 안의 object 참조(FNetObjectReference)
Attachments는 object에 부착되어 나가는 NetBlob, 실질적으로 RPC
Exports는 상대가 참조를 해석하는 데 필요한 부속 데이터(경로 문자열 같은 NetToken류)

---
---
---

FReplicationWriter
  FReplicationInfo

FReplicationReader
  FReplicatedObjectInfo 

---

FReplicatedObjectInfo

참조 해석의 지연 처리
UnresolvedObjectReferences / ResolvedDynamicObjectReferences

미해석 변경을 UnresolvedChangeMaskOrPointer 에 누적

---
---
---

SubObjectInternalIndices 
subobject인 모든 internal index의 bitset

자체 scope/filter/우선순위 없이 root object의 batch에 묶여 원자적으로 복제
수명도 root에 종속

---

DependentObjectInternalIndices 
ObjectsWithDependentObjectsInternalIndices

subobject와 달리 독립적인 replicated object인데 전송 스케줄만 parent에 결합된 것

AddDependentObject(FNetRefHandle Parent, FNetRefHandle DependentObject)
특별한 내장 관계가 아니라, 의존을 선언할 때 지정하는 임의의 다른 replicated object

등록하면 NetDependencyData에 parent → dependent 목록으로 저장

ex) PlayerController가 Pawn을 possess할 때
클라이언트가 Controller를 받았을 때 Pawn이 아직 없으면 참조가 미해석 상태로 남습니다. 
그래서 "Pawn을 Controller보다 먼저 스케줄하라"고 묶는 것


AddDependentObjectWithParentRelevancy
parent가 relevant할 때만 relevant

---

소유 클래스 안의 private 중첩 struct

FReplicationWriter::FReplicationInfo
per-connection object send state

FReplicationReader::FReplicatedObjectInfo
per-connection object receive state

---

FReplicationRecord
connection당 하나 있는 in-flight 원장(journal)

FRecordInfo와 FRecordInfoList는 FReplicationRecord 클래스 안의 중첩 타입

PushRecord(InfoCount) / PopRecord()가 packet 단위의 기입·소거
내부는 FRecordInfo들의 저장소 + object별 체인

---

Params.bIsServer              = !bInitAsClient;
Params.bAllowObjectReplication = !bInitAsClient;

클라이언트의 ReplicationSystem은 bAllowObjectReplication = false로 만들어지고
NetUpdate 안의 object 복제 블록 전체(poll → quantize → filtering → prioritization → scope 전파)가 
if (bAllowObjectReplication)로 감싸져 통째로 건너뜁니다

---

클라의 Writer는 주로 attachment(RPC)

UReplicationSystem::SendRPC로 들어온 Server RPC가 NetBlob이 되어 Writer의 송신 경로를 타고
자기가 복제하지 않는 object에 붙는 attachment를 위해 
특수 internal index(AttachmentToObjectNotInScope)까지 준비되어 있습니다

---

▼ WriteData(Context ↓, Record ↑)     ─ 송신
Channel ⇄ Manager ⇄ Stream ⇄ Writer/Reader
          ▲ ReadData(Context ↓)                ─ 수신
          ▼ ProcessPacketDeliveryStatus(Status, Record ↓) ─ 배달 통지

다형성 지점이 Manager→Stream 한 곳뿐이고, 
그 지점이 ini로 열려 있습니다

---
---
---

화물이 도착하는 항구

운송 포장 상태(quantized — 부피가 작고, 스캐너로 훑어 비교하기 좋은 형태)
개봉된 실물(external — FVector 같은 실제 타입, 게임 코드가 만질 수 있는 형태)
집에 배치된 상태(게임 오브젝트의 property — 실제로 사용되는 곳)

항구의 보세창고(ReceiveStateBuffer — 도착 화물을 포장 그대로 보관)
접이식 개봉 작업대(임시 external staging — MemStack에서 폈다가 접음)
집(게임 오브젝트)

---

배(packet)가 들어오면 화물은 일단 포장 그대로 창고에 입고됩니다
(Reader가 ReceiveStateBuffer에 직접 deserialize). 

배 한 척을 다 내린 뒤에야
(packet 완독 후 dispatch) 

창고에서 상자를 꺼내 작업대에서 개봉하고(dequantize), 
기사(fragment)가 집 안 제자리에 배치하고(apply), 
입주 절차(PreApply → Apply → PostApply/RepNotify)를 정해진 순서로 밟습니다.

---

어떤 화물은 "지난번 화물에서 달라진 부분만" 보내옵니다(delta compression)
복원하려면 지난번 화물(StoredBaselines)이 창고에 있어야 합니다

조립 지시서에 "부품 B는 별도 화물로 옵니다"라고 적힌 상자가 있습니다
(미해석 object reference)
가 도착할 때까지 상자를 창고에 두고, 도착하면 그 상자만 다시 꺼내 개봉·조립합니다
(UnresolvedChangeMask가 어떤 상자가 대기 중인지 표시)

---

새 부품으로 교체하기 전에 옛 부품을 보고 싶어하는 사람
(이전값을 인자로 받는 RepNotify)이 있을 때만, 옛 상태 상자를 잠깐 챙겨둡니다

PrevReplicationState, "필요할 때만 들고 다니는" 상태

---

개봉 담당자(serializer)는 상자를 "표준 진열 규격"(descriptor가 정의한 external state struct)까지만 풀 줄 알고, 
그 물건이 이 집의 어느 방 어느 선반에 가는지(object property layout)는 기사(fragment)만 압니다. 

두 담당자의 인수인계 지점이 작업대입니다. 

또 반쯤 연 상자를 거실에 두지 않는다는 규칙(원자성 — 게임 코드가 어중간한 상태를 관측하면 안 됨)과, 
지연된 상자를 몇 번이고 다시 개봉할 수 있어야 한다는 요구(반복 가능성) 때문에도, 
창고→집 직행이 아니라 "매번 새로 펴는 작업대"가 맞는 형태입니다

접이식(MemStack)이라 유지비는 0입니다.

---
---
---

Trait

UClass reflection을 hot path에서 걷어내는 대신, 
등록 시점에 descriptor/protocol이라는 타입 기술을 만듭니다

그런데 타입에는 레이아웃만 있는 게 아니라 성질이 있습니다
"이 타입은 가변 크기 메모리를 갖는가", "참조를 품는가", "RepNotify가 있는가"

파이프라인의 안쪽 루프는 오브젝트도 가상함수도 없이 버퍼와 descriptor만 봅니다
"이 state에 동적 메모리가 있나?"를 그 자리에서 알아내려면 
가상 호출이나 reflection 질의가 필요한데, 둘 다 금지된 세계입니다

그래서 모든 질문을 등록 시점에 미리 물어 bit로 저장해 두고, 실행 시점에는 bit test 하나로 경로 전체를 건너뜁니다

---
---
---

내륙에 공장(게임 오브젝트, game thread)이 있습니다. 
항구에는 수출 창고(ReplicatedObjectStateBuffers)가 있어서 
모든 수출품의 규격 포장본을 1부씩 보관
공장 상태를 비추는 거울

부두에는 목적지(connection)마다 배가 뜹니다.
공장은 물건을 바꿀 때마다 벨을 누릅니다 — "3번 라인 바뀜"(MARK_PROPERTY_DIRTY → DirtyNetObjectTracker의 bit 하나). 
벨을 안 다는 구식 공장(legacy mode)만 검수원이 주기적으로 돕니다(poll). 

출항 준비 시간(NetUpdate)이 되면 항구는 벨이 울린 물건만 가져와 재포장을 한 번 합니다(quantize). 
포장은 물건당 1회, 선적은 목적지 수만큼. 100척의 배가 떠도 포장대는 한 번만 돕니다.

---

목적지마다 서류가 따로 있습니다. 
수출 허가 목록(filtering → ObjectsInScope: 이 품목을 이 나라에 보낼 수 있는가), 
선적 우선순위표(prioritization — 배는 적재량이 한정이라, 오래 못 실린 화물일수록 누적 점수로 앞줄에 서고, 
"이 화물은 저 화물보다 먼저/같이"라는 동반 조항도 있습니다 — dependent objects). 

---

그리고 각 목적지별 거래 대장(FReplicationInfo)이 있습니다

이 거래처에 아직 못 보낸 변경 목록(accumulated mask), 
그리고 거래 상태 — 첫 거래라 카탈로그와 사업자 등록(creation header, archetype 정보)을 동봉해야 하는 단계
(PendingCreate → WaitOnCreateConfirmation), 
정상 거래 중(Created), 
거래 종료 절차 중(PendingDestroy → WaitOnDestroyConfirmation).

---

상대가 "지난번 화물 잘 받았음"을 확인해준 경우, 
그 확인된 선적 사진(acked baseline)을 기준으로 "달라진 부분만" 싸서 보냅니다(delta)

확인 안 된 사진은 기준으로 못 씁니다
상대 창고에 없을지도 모르는 물건과의 차이는 무의미

한 배에 안 들어가는 초대형 화물은 분할 선적하고(HugeObject), 
내용물이 "그 순간 그대로" 전달되어야 하는 특송 등기(RPC attachment)는 
접수 순간 봉인해서(호출 시점 quantize) 도착 확인이 올 때까지 재발송 목록에 둡니다.

---

배가 떠날 때마다 선하증권을 끊습니다(FRecordInfo → FReplicationRecord)
"이 배에, 이 화물의, 이 변경분(changemask 사본)을 실었음."

배가 침몰하면 어떻게 하나 
그때 그 상자를 다시 만들지 않습니다. 
선하증권을 펼쳐 "그 배에 실렸던 품목" 중 "이후 배에 이미 다시 실린 것"을 지우고(AndNot), 
남은 품목만 거래 대장의 미선적 목록에 되돌립니다(OrOp). 

다음 배에 실리는 건 창고의 최신 포장본입니다. 
고객이 원하는 건 잃어버린 그 상자가 아니라 지금의 물건

---

수신측 항구와의 대비로 요약하면
수신측 창고는 "도착했지만 아직 소화 안 된 진실의 대기실"이었고, 
송신측 창고는 "공장을 방해하지 않고 몇 번이고 실어 보낼 수 있는 진실의 거울"

---

상대 항구는 "그 배 침몰했어요"라는 전보를 보내지 않습니다. 
대신 상대는 자기가 보내는 모든 답신의 겉봉에 수령 명단을 적어 보냅니다
"지금까지 받은 가장 최신 배 번호, 그리고 최근 배들의 수령 여부 체크리스트."

NAK은 이 명단의 구멍을 보고 우리 쪽이 내리는 선고입니다. 
"7번, 8번은 받았다는데 6번 칸이 비어 있고, 명단이 이미 6번을 지나쳤다"

상대가 보낸 packet 도착
 └ UNetConnection::ReceivedPacket
    └ PacketNotify.Update(Header, HandlePacketNotification)
        Header = { 상대의 송신 Seq, 상대가 받은 최신 AckedSeq, 수령 여부 History 비트열 }
        → 지난 판정 지점(LastNotifiedPacketId)부터 배 번호를 하나씩 순서대로:
            bDelivered ? ReceivedAck(id) : ReceivedNak(id)

 └ UDataStreamChannel::ReceivedAck / ReceivedNak(PacketId)
    └ WriteRecords(FIFO) 선두의 PacketId와 일치 확인
    └ DataStreamManager->ProcessPacketDeliveryStatus(Delivered | Lost, record)
        └ 각 stream이 자기 몫의 record 해석 → Writer의 HandleDelivered / DroppedRecord

---
---
---

[송신]                                                         [수신]
게임 오브젝트                                                게임 오브젝트
   │ ① Poll  (비교+복사+bit set)                                 ▲ ④ Apply/Push (+RepNotify)
   ▼                                                              │
external ─ SrcReplicationState (상주, fragment 소유)             external ─ 임시 staging (MemStack, dispatch 수명)
   │ ② Quantize (dirty member만)                                 ▲ ③ Dequantize (dispatch 시)
   ▼                                                              │
internal quantized buffer (상주) ── serialize ▶ bitstream ▶ deserialize ── ReceiveStateBuffer (상주)

---

매 프레임 NetUpdate의 CallPreSendUpdate 단계
filtering 후, quantize 전

"이번 프레임 poll 대상" bit array
dirty 통지된 것 ∪ poll 주기가 도래한 것 ∪ 강제 poll — 를 만들고
FObjectPoller::PollAndCopyObjects가 일괄 순회

---

FPropertyReplicationState
  PollPropertyReplicationState(SrcData)
    게임 오브젝트의 property 값을 자기 사본과 비교
    다르면 사본에 복사하고(= external에 쓰기)
    그 member의 changemask bit를 set

송신측 external이 임시가 아니라 상주인 이유
fragment가 SrcReplicationState를 계속 들고 있는 것은
비교하려면 이전 값이 필요하기 때문

---

명시적으로 선언한 native replication state는 
external struct(changemask가 내장된) 자체를 게임 코드가 직접 쓰고, setter가 bit를 직접 마킹

---

Dispatch가 일어나는 순간(packet 완독 후, 또는 미해석 참조가 해석된 순간)
DequantizeAndApplyHelper가 ReceiveStateBuffer에서 임시 external을 만들고
그 자리에서 fragment에 넘깁니다

---

ApplyReplicatedState

이미 만들어진 external 버퍼(Context의 ExternalStateBuffer)를 FPropertyReplicationState로 감싸기만 하고
이전값 RepNotify가 필요한 모드면 apply 직전에 오브젝트의 현재 값을 PrevReplicationState에 백업하고
PushPropertyReplicationState
descriptor의 지도를 따라 state struct의 값을 property 단위로 오브젝트 메모리에 복사
RepNotify 호출은 그 다음 단계

---
---
---

이 데이터를 매 프레임 누가, 어떤 패턴으로 훑는가
루프의 주인이 데이터의 주인
이렇게 하면 각 루프는 자기 컬럼만 조밀하게 훑을 수 있다(cache)


ReplicatedObjectData[i]                                   // 핸들·프로토콜·플래그·ReceiveStateBuffer
FNetRefHandleManager::ReplicatedObjectStateBuffers[i]     // 송신 quantized 현재본
FReplicationStateStorage::FPerObjectInfo[i]               // 현재본 "참조" 등록 + baseline 스냅샷 "소유"

---

FReplicationWriter (×C)  FReplicationInfo[i] // 거래처별: 뭘 아직 못 보냈나
FReplicationReader (×C)  FReplicatedObjectInfo[i] //거래처별: 뭘 받는 중인가

---
---
---

────────── 하나의 항구 (ReplicationSystem) ──────────

[수출부 — 내가 진실의 주인인 오브젝트]       [수입부 — 남이 주인인 오브젝트]

 공장 = 게임 오브젝트 (권위)                   집 = 게임 오브젝트 (수신본)
   │ 벨 = DirtyNetObjectTracker                ▲ Push (+ 옛부품함 = PrevReplicationState, 필요시만)
   ▼ Poll                                      │
 견본실 = SrcReplicationState (external 상주)  접이식 작업대 = temp external (MemStack)
   │ Quantize                                  ▲ Dequantize (dispatch 때만)
   ▼                                           │
 수출 창고 =                                  수입 창고 = ReceiveStateBuffer
   ReplicatedObjectStateBuffers[i]              ├ 지난 화물 사진 = StoredBaselines[2]
   │                                            └ 대기 표시판 = UnresolvedChangeMask
   ├ 오늘의 갱신 공고판 = FChangeMaskCache
   ├ 거래처별 거래 대장 = Writer FReplicationInfo (항로 수만큼)
   ├ 선하증권 철 = FReplicationRecord/FRecordInfo (항로 수만큼)
   └ 특송 접수대 = NetBlobManager attachment 큐 (창고를 안 거침)
   ▼
 부두 사무소 = DataStreamChannel (출항 기록 FIFO = WriteRecords)
   ▼                                            ▲
   배(packet) ───────── 항로 = connection ──────┘

        [두 날개 사이 복도: 창고업자 = FReplicationStateStorage]
        (수출 창고든 보세창고든 CurrentSend/CurrentRecv라는 하나의
         주소 체계로 접근하며, baseline 원판 보관실을 직접 운영)

---

CreateNetObject 

수출창고 칸 ReplicatedObjectStateBuffers[i])이 할당되고 
수입창고 칸 (ReceiveStateBuffer)는 null로

CreateNetObjectFromRemote 

회선 저편의 creation 정보로 남의 진실을 본떠 생성된 오브젝트
반대로, 보세창고만 할당

---
---
---

"지금 이 오브젝트의 quantized 상태를 사진 찍어 보관해 달라"는 요청이 시스템 곳곳에서 나옵니다. 
송신측 delta compression이 그렇고(ack된 시점의 상태 사진 = baseline), 
수신측 Reader도 그렇습니다(받은 상태의 사진 = StoredBaselines). 

첫째, 원본의 위치가 오브젝트마다 다릅니다
내가 주인인 오브젝트면 수출 창고(ReplicatedObjectStateBuffers), 
남이 주인이면 보세창고(ReceiveStateBuffer). 

사진을 원하는 쪽마다 이 구분을 알게 하면 비대칭이 코드 전체에 번집니다. 

해법 = FReplicationStateStorage

(a) 안내 장부: 오브젝트마다 원본 포인터를 두 칸(FPerObjectInfo의 Send칸/Recv칸)에 등록해 두고, 
손님에게는 GetState(오브젝트, CurrentSendState 또는 CurrentRecvState)라는 단일 창구만 보여줍니다. 

없는 쪽을 물으면 null이 답입니다("수신측에서 CurrentSendState를 요청하면 null")

원본은 소유하지 않습니다 — 어느 서랍에 있는지 아는 것뿐

---
---
---

FNetRefHandleManager (등기소)
  ├ ReplicatedObjectData[i]
  │   └ .ReceiveStateBuffer ─ CreateNetObjectFromRemote가 malloc   [보세창고]
  └ ReplicatedObjectStateBuffers[i]
      └ 각 원소 ─ CreateNetObject가 malloc                         [수출 창고]


FReplicationStateStorage 
  ├ FPerObjectInfo[i].StateBuffers[Send|Recv] ─ 위 둘의 등록된 참조
  └ baseline 원판 메모리 ─ AllocBaseline로 대여                     [사진 원판 보관함]
      (대여자: 송신측 DeltaCompressionBaselineStorage · 수신측 Reader)


FReplicationConnection[ConnId]
  ├ FReplicationWriter 
  │   ├ FReplicationInfo[i] ─ .ChangeMaskOrPtr                 [거래 대장·누적 mask]
  │   ├ FReplicationRecord ─ FRecordInfo들의 changemask 사본   [선하증권 철]
  │   └ attachment 큐                                          [특송 보관대]

  ├ FReplicationReader (new/delete로 직접 소유)
  │   └ FReplicatedObjectInfo[i]
  │       ├ .UnresolvedChangeMaskOrPointer                      [대기 표시판]
  │       └ .StoredBaselines[2] ─ 복사집에서 대여               [지난 화물 사진]


---

Bridge ─ FReplicationInstanceProtocol (오브젝트마다, EndReplication 때 파기)
  └ FPropertyReplicationFragment 
      ├ SrcReplicationState ─ 내부 external 버퍼                    [견본실]
      └ PrevReplicationState ─ 필요할 때만                          [옛 부품함]

---

보세창고의 소비자는 사실상 Reader/dispatch 하나
접근은 "batch가 도착한 그 오브젝트"에 대한 점(point) 접근
그 순간에는 어차피 같은 행의 protocol 포인터, flags, factory id를 함께 읽어야 합니다

여러 시스템이 대량 순회하는 데이터는 컬럼으로 꺼내고
한 시스템이 이웃 필드와 함께 점 접근하는 데이터는 행에 묻는다

배치가 접근 패턴

---

이 시스템의 자기 인식에서 기본 서사는 권위측(송신)
기본 저장소에는 방향 표시를 붙이지 않았습니다

Storage가 서비스 API를 만들면서 선택한 어휘는 CurrentSendState / CurrentRecvState
방향을 명시

---

수출 창고 배열을 통째로 받아 가는 소비자
quantize, connection별 serialize에 더해 Filtering과 Prioritization

---
---
---
---
---

같은 대상의 상태를, 조금씩만 바뀌는데, 계속 반복해서 보내야 한다. 그렇다면 "반복"을 어디까지 제거할 수 있는가

0단: 매번 전체 상태를 보낸다
1단: 바뀐 멤버만 보낸다. 이게 changemask입니다
2단: 바뀐 멤버조차, 통째로 말하지 않고 "기준 대비 차이"로 말한다. 이게 delta compression입니다

---

인벤토리 100칸 중 1칸이 바뀌어 bit가 서면, 1단만으로는 100칸을 다 보냅니다.
2단이 있으면 99칸은 "기준과 같음" 1비트씩으로 끝납니다


값이 A→B→A로 진동했거나, packet 유실로 changemask가 OR-back되어 되살아난 경우, 
bit는 서 있지만 값은 기준과 같을 수 있습니다

---

규칙 1: "받았어"라고 답장 온 편지만 기준으로 삼는다
상대의 수령 확인만이 "우리 둘이 같은 과거를 공유한다"의 증명
기준(baseline)은 ack된 것만. 그 전까지는 후보(pending)일 뿐

---

규칙 2: 기준 편지의 사본을 나도 보관한다. 

내 근황(현재 상태)은 계속 변합니다. 
3주 뒤 "그 편지 기준으로 뭐가 달라졌지?"라고 물으려면 
그때 뭐라고 썼는지가 남아 있어야 합니다

송신측이 사진을 찍어 동결하는 이유

---

규칙 3: 친구도 편지를 버리면 안 된다

"지난번 대비 달라진 것"이라는 문장은 지난 편지를 옆에 펴 놓아야만 해독됩니다
수신측 StoredBaselines

---

규칙 4: 기준이 없으면 그냥 전부 쓴다

첫 편지, 또는 기준 편지가 분실 판정
언제든 절대값 폴백이 가능

---
---
---

기준은 connection별
친구가 열 명이면 수령 확인 시점이 제각각
오브젝트당 사진 하나가 아니라 거래처마다 따로

사진 = POD 버퍼의 복제
"기준과 같은가?" = memcmp
상태를 게임 타입 그대로 두었다면 비교도 복제도 이렇게 값싸지 않습니다

---

사진 한 장 = 오브젝트 전체 상태 크기 × 거래처 수 × 최대 2장

---
---
---

이름의 경제

0단: 매번 전체 경로를 보낸다

1단: 양쪽이 합의한 짧은 번호로 바꾼다. 
단, 첫 만남에는 "번호 = 이 이름"이라는 정의를 한 번은 보내야 합니다. 이게 export입니다.

2단: 정의를 "딱 한 번만" 보내려면 상대가 정의를 받았는지 알아야 합니다. 
못 받았는데 번호만 보내면 해독 불능이니까요
ack만이 화폐입니다
그래서 export에도 자기 장부가 필요합니다

3단: 이름에 두 종류
살아있는 복제 오브젝트는 이미 번호가 있습니다 — NetRefHandle
경로로만 지칭 가능한 것(asset, 레벨의 정적 오브젝트)은 이름을 접어 번호로 만듭니다 — NetToken

---
---
---

FNetObjectReference

RefHandle
주소 약어(PathToken)
"이 명함은 처음 보는 사람에게 소개 가능"(CanBeExported) 도장

사람을 찾는 방법의 기술

---

FObjectReferenceCache

소개(export)할 때는 상사의 상사까지 거슬러 올라가 체인 전체를 소개
OuterNetRefHandle

주소란 결국 "누구 안의 누구"

---

약어 사전은 공용이 아닙니다
양쪽이 같은 데이터에 서로 다른 로컬 번호를 매길 수 있다

수신자는 "상대의 사전"을 따로 보관
FNetTokenStore의 RemoteNetTokenStoreStates[ConnectionId]

---

명함을 받았는데 그 사람이 아직 입사 전일 수 있습니다(오브젝트 미생성)
주소의 건물이 아직 건설 중일 수 있습니다(package 미로드)

그래서 명부 항목에 pending/broken/bIgnoreWhenMissing 같은 상태 bit가 있고
async load 중에는 GC가 건물을 철거하지 못하게 strong reference로 붙들며(FQueuedBatchObjectReference)

"이 명함이 해석될 때까지 이 화물은 개봉 금지"(must-be-mapped)면 
batch째 PendingBatches에 보류

---
---
---

RPC는 상태가 아니라 사건(event)

최신값으로 대체될 수 없고, 두 번 실행되면 안 되며, 종종 순서까지 지켜져야 하고
호출 순간의 인자 그대로 전달되어야

"최신값이 이긴다"로 설계된 파이프라인에, "말한 그대로, 한 번만, 순서대로"를 요구하는 화물을 어떻게 싣는가

---

사건을 오브젝트에 부착(attachment)하여, 오브젝트가 이미 가진 주소·scope·배송망에 승차시킨다.

부착하려면 사건을 화물의 형태로 동결해야 합니다

UFunction의 인자 목록이란 결국 struct
그렇다면 함수 인자에도 FReplicationStateDescriptor를 만들면, 
상태를 위해 지은 기계 전부 — quantize, serializer, trait, 참조 수집(exports) — 를 씁니다

RPC는 "일회용 replication state"

---

함수의 신원 
이름 문자열이 아니라 스키마 좌표
FunctionLocator = {protocol 내 descriptor index, descriptor 내 function index}

---

FNetBlob
  취급 스티커: Flags ( Reliable / Ordered / 소개장 HasExports)
  BlobDescriptor        
  QuantizedBlobState

FNetObjectAttachment
  NetObjectReference       
       
FNetRPC
  FunctionLocator {descriptor 몇 번, 함수 몇 번}
  호출 인자 스냅샷 (+ 인자 속 참조들의 소개장 목록)

---

게임 코드가 SendRPC를 부르는 순간 인자가 즉시 quantize되어 봉인됩니다
호출 뒤 인자 원본이 바뀌어도 소포 속은 그대로

소포는 먼저 접수대(FNetBlobManager의 게임 스레드 큐)에 쌓입니다

PreSendUpdate에서 접수대가 비워지며 
scope·정책 검사를 거쳐 각 항로 사무소의 발송대(Writer의 FNetObjectAttachmentsWriter
오브젝트별 reliable/unreliable 창구,
OutOfBand·HugeObject 전용의 특수 창구)로 분배

---

거기서 오브젝트 batch에 동승해 packet에 실리고
도착하면 Reader가 창구 번호로 개봉 담당(UNetRPCHandler)을 찾아 넘기고
담당이 수취인과 함수를 해석하고 authority 검증을 거쳐 UFunction을 실행합니다

---

dormant 오브젝트에 RPC를 보내면 정책에 따라 그 오브젝트의 상태 flush를 자동으로 깨웁니다
RPC가 도착했는데 그 대상의 상태가 한참 뒤처져 있으면 사건이 무의미해질 수 있으니까

---
---

전부 등기소에 넣으면 등기소의 캐시라인은 온갖 소비자의 데이터로 오염됩니다
신상, 위치, 우선순위, 사진 원판, 그룹, 조건

---

오브젝트의 출생·사망과 원자적으로 함께 갱신되어야 하는가?
그렇다면 등기소 안
CreateNetObject / CreateNetObjectFromRemote

다른 곳에 두면 "태어났는데 아직 등록 안 됨"이라는 찢어진 순간이 생깁니다

---

특정 정책의 소관이며, 교체·비활성·치수조정이 가능한가?

Filter는 플러그인(UNetObjectFilter 파생들)
prioritizer도 교체 가능
delta compression은 opt-in에 예산(2048)이 있고
WorldLocations는 "위치"라는 특정 입력의 캐시 서비스
Groups는 별도 개념 축

---

Index 공간이 자라면(오브젝트 수가 선할당을 넘으면) 
등기소가 OnMaxInternalNetRefIndexIncreased를 방송하고, 
그 수신자 목록이 곧 세입자 명부

Conditionals, DirtyTracker, DC Manager
InvalidationTracker, Groups, Filter

등기소는 index 공간의 땅주인이고, 
부처들은 그 좌표 위에 자기 컬럼을 세든 세입자라서, 
땅이 넓어지면 각자 자기 대장을 늘려야 합니다

소유권이 흩어져 있어도 좌표 체계는 하나

---

FReplicationSystemInternal = 내각 (부처를 값으로 품은 캐비닛)
│
├─ FNetRefHandleManager = 등기소 (땅주인)
│
└─ 부처들 (세입자: 주민번호를 키로 각자 대장 유지)
     Filtering, Prioritization, WorldLocations,
     Groups, DC Manager(사진 원판 보관소), Conditionals,
     DirtyTracker, StateStorage(복사집), NetBlobManager,
     Connections(항로별 사무소 → 그 안에 Writer/Reader)

---
---
---

net.Iris.ExecuteReliableRPCsBeforeApplyState=true
이미 존재하는 객체의 Reliable RPC에만 적용

현재 Health=100이고 다음 데이터가 함께 도착했다면

Health = 50
ClientHitReaction() Reliable RPC

ClientHitReaction() 실행   // 아직 Health == 100
Health = 50 적용
OnRep_Health() 실행

---

한 프레임
그 함수가 프레임 시간표의 어디에 사는지

시계

필터를 바꿔도 되는 시간
게임 메모리를 만져도 되는 시간
packet을 써도 되는 시간

---

UNetDriver
TickDispatch(수신 처리)
PostTickDispatch

---

Pre-/Post-는 괄호

PreSendUpdate/PostSendUpdate
PreReceiveUpdate/PostReceiveUpdate

Begin/End는 괄호 중에서도 트랜잭션 괄호
BeginWrite/EndWrite
커밋/롤백 의미(bitstream rollback scope, record 확정)를 약속

---

ensureMsgf(CurrentSendPass == Invalid, "PostSendUpdate was not called after the last Tick")
일종의 시간 타입 시스템

---

구동의 방향

UpdateFiltering, UpdateConditionals — 파이프라인의 능동 단계
OnStartPreSendUpdate, OnPostSendUpdate, OnMaxInternalNetRefIndexIncreased - On은 "통지받는다"

---

게임 메모리를 만지는 구간들이 Call- 함수들
외부(게임) 코드를 호출한다

CallPreSendUpdate, CallRepNotifies, CallPostApplyInitialState

---

                Send                         Receive
  Pre    NetUpdate (≒PreSendUpdate)     PreReceiveUpdate
  본체   SendUpdate                    (채널의 ReceivedBunch→ReadData)
  Post   PostSendUpdate                 PostReceiveUpdate

송신 본체는 채널의 Tick→WriteData가
수신 본체는 채널의 ReceivedBunch→ReadData가 구동

SendUpdate에는 "미래에 ReplicationSystem이 직접 send를 맡을 자리의 placeholder"

---
---
---

Dispatch
들어온 것을 담당 처리자에게 라우팅하다

virtual dispatch
event dispatcher
interrupt dispatch

TickDispatch

socket에서 packet을 읽고
그 packet을 connection으로,
bunch를 channel로, 
내용물을 handler로 분배하는 것

---

수신이란 "받았다"에서 끝나지 않고 "제 주인에게 배달 완료했다"까지
배가 닿는 게 수신이 아니라, 하역한 화물을 각 부서로 파견하는 것까지

---

TickFlush

네트워크 송신
본질적으로 buffered write

printf는 보내는 행위가 아니라 버퍼에 쌓는 행위고, 실제로 내보내는 것은 fflush(stdout)

프레임 내내 게임 코드는 dirty 표시를 쌓고 RPC를 큐에 넣을 뿐
프레임 자체가 하나의 쓰기 버퍼

---

driver가 자기 버퍼에 하는 일의 어휘
TickDispatch와 TickFlush
send/receive라는 wire의 어휘가 아니라
이름의 주어가 회선이 아니라 버퍼

---

SendUpdate
송신 회차

PreSendUpdate
Pre-(SendUpdate)
송신 회차의 앞괄호

---

packet은 프레임을 모르고 아무 때나 도착합니다
박자 불일치(impedance mismatch)

박자가 다른 두 세계를 잇는 보편적 도구는 버퍼입니다
OS가 socket receive buffer에 쌓아둡니다

게임은 이산적입니다 — 프레임 단위로만 세계를 갱신합니다

NetDriver
외부 박자로 쌓인 것을 프레임 박자에 맞춰 분배하고
(TickDispatch — 프레임 시작에 OS 버퍼를 비워 각 connection/channel로)

내 박자로 쌓인 것을 회선의 박자로 방류한다
(TickFlush — 프레임 끝에)

---
---
---


프레임 동안 쌓이는 것

dirty bit 하나(값이 아니라 "값을 보내야 한다는 어음")
pending create/destroy 표시

프레임은 완성품을 쌓는 게 아니라 미뤄둔 출력 의무의 원장을 쌓습니다

MARK_PROPERTY_DIRTY가 1비트만 쓰는 이유
쓰기 순간은 싸게, 정산은 나중에 몰아서
몰아서 정산하기 때문에 합쳐집니다
한 프레임에 HP가 다섯 번 바뀌어도 어음은 한 장, 결제는 최종값 한 번

---

TickFlush의 flush

지연시킨 출력 의무의 강제 완결

그 완결에 제조(poll → quantize → serialize)가 포함되는 것은
지연 평가(lazy evaluation)의 핵심

---
---
---

Dispatch는 충실의 세계

도착한 packet의 내용과 순서는 발신자가 이미 결정했고, 
수신자에게 허용된 것은 정확한 해석뿐
(선별도 병합도 예산도 없습니다 — 유일한 재량인 "미해석 참조 보류"조차 '언제 적용하나'이지 '무엇을 적용하나'가 아닙니다)
송신자는 작곡가고 수신자는 연주자입니다.

---
---
---

Impedance
1886년 Oliver Heaviside
어떤 매질이 흐름 1단위를 통과시키는 데 얼마의 압력을 요구하는가 비율
모든 매질은 이 고유 비율을 갖습니다

두 매질을 이어 붙일 때 일어나는 일

신호가 임피던스 50Ω짜리 케이블을 달리다가 75Ω짜리 장비를 만나면,
신호 전체가 건너가지 못하고 일부가 경계에서 반사되어 되돌아옵니다. 

반사파는 원래 신호와 겹쳐 왜곡(메아리, 정재파)을 만듭니다. 

반대로 양쪽 비율이 일치하면(matched) 신호는 경계를 느끼지 못하고 전부 통과

---

첫째, 모든 매질/시스템은 고유한 특성 비율을 갖는다
둘째, 비율이 다른 두 시스템의 경계에서는 전달이 불완전하다
셋째, 해법은 변압기(transformer)다

비율을 변환해 주는 중간 장치
전기의 변압기, 자전거의 기어비
초음파 검사의 젤

넷째, 급격한 경계 하나보다 완만한 경계 여럿이 낫다

금관악기의 나팔이 점점 벌어지는 이유는 
관 내부와 바깥 공기의 임피던스를 점진적으로 정합해 
소리가 반사 없이 빠져나가게 하기 위해서

안경의 반사방지 코팅 
유리와 공기 사이에 중간 굴절률 층을 끼워 넣는 기법

인체의 중이(中耳)에 뼈가 세 개(망치뼈·모루뼈·등자뼈)
공기(낮은 임피던스)와 달팽이관 림프액(높은 임피던스) 사이의 단계적 변압기

---

1990년대의
object-relational impedance mismatch

객체 세계(그래프, identity, 상속, 행위)와 관계형 세계(집합, 값, join, 정규화)
둘을 이으면 경계에서 "반사"가 일어납니다

그 반사의 소프트웨어적 형태가 glue code의 부피, 어색한 매핑, N+1 쿼리

ORM은 변압기

---

이후 이 어휘는 모든 종류의 경계로 일반화됐습니다
동기/비동기, push/pull, 스트림/배치, 이벤트 기반/프레임 기반, 가변/불변, 행 지향/열 지향

회선은 "아무 때나 도착"이라는 박자를, 
게임은 "프레임마다 한 번"이라는 박자를 갖고, 
버퍼는 시간의 변압기
댐이 불규칙한 강우를 일정한 급수로 바꾸듯이

---
---
---

Stream 
연속성
하나

연속성은 stream이 지어내는 구축물

---

FReplicationWriter

비트를 쓰는 것(serialize)은 마지막 한 걸음이고
connection별 원격 지식 모델 + 수렴 엔진

ObjectsInScope(이 상대가 알아야 할 것)
FReplicationInfo의 상태기계(이 상대가 각 오브젝트를 아는 단계: 모름 → 소개했으나 미확인 → 앎 → 잊으라고 통보 중 → 잊음)
accumulated mask(상대의 앎과 현재 진실 사이의 알려진 격차)
acked baseline(상대가 갖고 있음이 증명된 지식의 스냅샷)
ReplicationRecord(그 앎을 갱신하러 날아가는 중인 전언들)

---

구독자마다 전담 편집자(Writer)가 붙습니다
그 구독자에 대한 서류철이 놓여 있습니다

이 분의 관심 범위(scope), 
이 분이 이미 아는 것과 모르는 것(상태기계), 
지난 발송 이후 세상이 달라진 목록(accumulated mask), 
이 분이 "잘 받았다"고 엽서로 확인해 준 기준판(acked baseline — 이후로는 "그 판에서 달라진 것만" 써도 통함), 
발송했지만 아직 엽서가 안 온 우편물의 발송 대장(record)

지면은 한정이라(예산) 중요한 것부터 싣고, 
오래 밀린 소식일수록 앞으로 당깁니다

---
---
---

Writer의 예산

MaxPackets 
채널이 connection의 포화도/송신 창을 보고 계산해 BeginWrite에 넘겨주는 값
PostTickDispatch 긴급 발송은 1로 고정

bunch 한도
Writer는 이 안에서 쓰다가 넘치면 그 batch를 rollback하고 
다음 bunch으로 이월

ReplicationRecord ring이 가득 차면 더 보낼 수 없습니다
보낸 것을 기록할 수 없으면 보내면 안 되니까

---

prioritizer가 정한 축적 속도(거리 기반 sphere, 정적 중요도 등)
보내지 못한 프레임만큼 쌓이므로
누적치 ≈ 중요도 × 기다린 시간

새로 태어난 오브젝트(CreatePriority 1.0), TearOff, 
유실로 재전송이 필요해진 것(LostStatePriorityBump)은 
즉시 문턱값(SchedulingThreshold 1.0)을 넘겨 앞줄에 세웁니다

---

scope ∩ dirty 후보를 모으고, 
dependent 오브젝트에 parent 우선순위를 전파하고,
partial sort(한 번에 최대 128개 후보 덩어리씩만 정렬 — 전체 정렬은 낭비이므로 예산만큼만 정렬)를 한 뒤, 
위에서부터 packet에 채워 넣다가 예산이 끝나면 멈춥니다

---

못 실린 오브젝트는 dirty인 채로 남아 다음 프레임에 더 굶주린 우선순위로 돌아옵니다

---

Destroy를 보내고 확인을 안 받으면
(WaitOnDestroyConfirmation 없이 잊어버리면), 
그 packet이 유실됐을 때 상대 세계에는 유령이 남습니다

종료 통보는 다른 어떤 상태보다도 reliable해야 합니다

---
---
---

Reader
내가 아직 할 수 없는 것의 목록
자신의 무지를 장부에 적는 자

지금 적용할 수 없는 것을 언제 다시 시도할 것인가
방금 실패한 참조는 매번 재시도(hot), 오래 실패한 참조는 고정 주기로만(cold).

UnresolvedChangeMask
Unresolved/ResolvedDynamic
PendingBatches

---

Writer는 자기 데이터를 신뢰하지만 
Reader는 신뢰하지 않습니다

Bit 수를 읽자마자 overflow와 상한(batch count ≥ 8192면 즉시 에러)을 검사하고, 
protocol 호환을 검증하고, 
RPC는 실행 전에 authority 검사를 통과해야 합니다

해석 불가능이 확정된 오브젝트는 BrokenObjects로 격리합니다
회선 너머는 신뢰 경계 밖이고
Reader가 그 국경의 검역소

---

packet을 끝까지 읽은 뒤에 dispatch하고
root → subobject 순서를 지키고
PreApply → Apply → RepNotify라는 단계를 가지고, 초기 상태면 PostApplyInitialState 훅까지

---

"지난 확정판 대비 변경분"으로 온 기사는 
보관 중인 확정판(StoredBaselines)을 펴서 완전한 기사로 복원

아직 인물 소개가 도착하지 않은 사람이 언급된 문단은 
자리를 비워둔 채 나머지를 먼저 조판(unresolved changemask — 빈자리 북마크)

---
---
---

Relevance
"이 (object, connection) 쌍에서, 이 connection이 이 object를 알아야 하는가?"

Filter
그 질문에 일괄로 답을 계산하는 기구

Scope
이 connection에 relevant한 것들의 집합
Writer의 ObjectsInScope bit array

Cull
NetCullDistance는 grid filter에 들어가는 입력 파라미터

---

Legacy에서 relevance는 actor의 속성
각 actor가 자기 판단 로직(가상함수)을 들고 있고, 매 프레임 connection마다 물어봤습니다

Iris는 그 판단을 actor에서 떼어내 중앙의 일괄 filtering 공정으로 바꿨고
공정의 산출물을 지속되는 집합으로 물화했습니다 (scope)

---

Scope는 "상대가 지금 사본을 갖고 있어야 하는 목록"

어떤 오브젝트가 scope에 들어오는 순간 = 구독 시작 = 상대에게 생성(creation)을 보내야 하고, 
나가는 순간 = 해지 = 파괴/정리를 보내야 합니다

Writer의 UpdateScope가 이전 scope와 새 scope를 diff해서 
create/destroy 전이를 만들어내는 게 그 구현

---

RelevantObjects = "어느 connection의 scope에든 들어 있는 것들의 합집합"
아무도 안 보는 오브젝트는 poll/quantize조차 하지 않기 위한 상위 게이트

NetUpdate에서 filtering이 poll보다 먼저 도는 이유

---
---
---

Writer
connection마다 하나

전체 object index 공간 [0, MaxInternalIndex)에 대한 ObjectsInScope bit array를 자기 것으로 가집니다
Writer는 같은 index 공간 크기의 배열을 여러 벌 들고 있습니다

FReplicationInfo[i](64bit 거래 상태)
SchedulingPriorities[i](float)
RecordInfoLists[i](in-flight 체인 머리)
ObjectsWithDirtyChanges/ObjectsPendingDestroy

---

                object 0   object 1   object 2  
connection A  [    ..         ..         ..      ] 
connection B  [    ..         ..         ..      ]

---

RecordInfoLists[i]

전표는 날짜철에 시간순으로 꽂습니다
동시에 거래처마다 실을 꿰어 둡니다
그 거래처의 미결 전표만 당기면 주르륵 딸려 나오게

물리적 보관은 하나, 색인은 둘(시간순 + 주제순)

자료구조 용어로는 ring buffer를 관통하는 intrusive linked list

---
---
---

(관심사 concern) × (connection) × (object)  →  값

시트 "scope"       : bit           시트 "priority"  : float
시트 "거래상태"    : 64bit 구조    시트 "baseline"  : 포인터 2칸
시트 "conditional" : bit들         시트 "token 사전": (object 축 퇴화)

시트 "현재 상태본" : (connection 축 퇴화 — 전역 테이블)

---

전역 per-object 테이블(등기소, WorldLocations)은 connection 축이 접힌 시트이고
connection당 하나뿐인 것들(ReplicationView, UserData)은 object 축이 접힌 시트입니다

---

시트 "scope 계산본"   (bit, 매 프레임 재계산, Filtering 소유)
시트 "scope 작업본"   (bit, 프레임 넘어 지속, Writer 소유 — 이전 프레임과 diff해 create/destroy 도출)

시트 "priority 계산본" (float, 재계산, Prioritization 소유)
시트 "priority 작업본" (float, 누적·리셋 상태, Writer 소유)

시트 "거래 상태"       (64bit struct = 상태기계+mask+baseline index, 지속, Writer 소유)
시트 "in-flight 실 머리" (index쌍 {first,last}, 지속, Writer 소유)
시트 "조건부 열람권"    (조건 mask, Conditionals 소유)


                 obj0  obj1  obj2 …
 connA          [  1     0     1   ]   ← ConnectionInfos[A].ObjectsInScope
 connB          [  0     0     1   ]


---

"진실은 하나"의 시트들

                obj0  obj1  obj2 …
connA          [  1     0     1   ] 

시트 "신상 명부"     FReplicatedObjectData[i]        (struct, 등기소)
시트 "수출 창고"     ReplicatedObjectStateBuffers[i] (uint8*, 등기소)
시트 "refcount"        [i] (uint16)   
시트 "인스턴스 역참조" [i] (TObjectPtr)
시트 "전역 집합들"   Scopable/Polled/DirtyToQuantize/SubObject/Dependent/Dormant… (bit ×15장)
시트 "주소지 대장"   WorldLocations[i]
시트 "민원 접수함"   AccumulatedDirtyNetObjects[i] (bit, DirtyTracker)
시트 "filter 배속"   NetObjectFilteringInfos[i]  ← "이 오브젝트는 어느 filter 소관인가" — 판정이 아니라 배속이라 connection 무관

---

항로 자체의 시트들

시트 "항로 슬롯"     FReplicationConnection[c] = {Writer*, Reader*, Manager약참조, UserData}
시트 "시점"          ReplicationView[c] (filter·prioritizer의 입력)
시트 "번역 사전"     RemoteNetTokenStoreStates[c]   ← object 축이 token 축으로 대체됨
시트 "export 장부"   NetExports[c]                  ← object 축이 packet(시간) 축으로 대체됨

---

FReplicationRecord는 [connection × packet순서]의 시트

---

인수분해

[object × group]      : FNetObjectGroups 멤버십 ("이 오브젝트는 레벨L 그룹 소속")
[group × connection]  : group filter status  ("레벨L은 connA에게 열람 허용")
     → 곱하면 [object × connection] 판정이 유도됨

큰 행렬을 저랭크 곱으로 압축
레벨 하나가 로드/언로드될 때 시트 한 줄만 바꾸면 수천 오브젝트의 판정이 일괄로 뒤집히는 이유

---
---
---

NetUpdate 전체를 데이터 흐름 그래프로 그리면 하나의 깔때기이고, 그 출구가 두 곳

공유 창고(quantized state buffers)
각 Writer의 행(작업본들)

인계 지점
UpdateScope(scope 계산본 → Writer의 scope 작업본, diff로 create/destroy 도출)
UpdatePriorities(priority 계산본 → Writer의 SchedulingPriorities)
PropagateDirtyChanges(이번 프레임 changemask → 각 Writer의 pending mask)

---

poll/quantize의 산출물은 Writer 외에 DC baseline과 export 수집도 먹고
WorldLocations는 Writer가 아니라 filtering/prioritization(Writer 상류)이 먹습니다
모든 상류가 packet의 순간을 향해 수렴하고, Writer는 그 마지막 역

---
---
---

"싼 컬럼 대수로 '비싼 작업의 대상 목록'을 극소로 줄인 뒤, 
비싼 작업(포인터 추적, 게임 메모리 방문)은 그 목록에만 한다."


오브젝트 65536개 = 2048 word = 16KB, 배열 서너 개를 
lockstep으로 AND/OR 해도 L1~L2 안에서 순식간
(명령 하나가 오브젝트 64개를 처리)

---

오브젝트당 포인터 추적 — 게임 인스턴스 방문, fragment 호출, 캐시 미스의 세계
아래층으로 위층의 횟수 n을 깎는 것
비싼 계산을 드물게 만듭니다

---
---
---

poll 대상 목록 만들기

재료(전부 bit 컬럼)
  Dirty                 ← 접수함(GlobalDirtyTracker) + ForceNetUpdate        [OR]
  Relevant              ← 모든 connection scope의 합집합                     [OR 여러 장]
  PollDue               ← ObjectPollFrequencyLimiter (주기 도래)             

조리:
  PolledObjects = (Dirty ∪ ForceNetUpdate) ∩ Relevant ∩ PollDue    ← word 단위 AND/OR

그 다음에 비싼 일
  PolledObjects의 켜진 bit마다: 
    ReplicatedInstances[i]로 게임 오브젝트 방문
    → PreUpdate 콜백 → fragment Poll (포인터 추적의 세계)

---
---
---

connection별 scope 만들기 (filtering)


공유 준비(오브젝트 축 1회)
  WorldLocations[i] 갱신 → grid filter가 위치 컬럼을 한 번 쓸어 cell에 binning

connection마다
  Scope[c] =  GlobalScopable
             ∧ ¬GroupExcluded[c]      ← (object×group)×(group×conn) 인수분해에서 유도
             ∧ DynamicResult[c]       ← view 주변 cell들을 모아 만든 bit 컬럼


100 connection × 2048 word ≈ 20만 word 연산
조 단위 사이클을 가진 CPU에게 미미한 로드

---

UNetDriver::TickFlush
  ReplicationSystem->NetUpdate

  각 UNetConnection::Tick(Dt)
    UChannel::Tick

  UNetDriver::PostTickFlush

---

DataStreamManager->BeginWrite
  UReplicationDataStream::BeginWrite
    FReplicationWriter::BeginWrite


while (쓸 게 남았고 && packet 예산 허용):
  DataStreamManager->WriteData
    FReplicationWriter::Write
  BitWriter.CommitWrites
  OutBunch.SerializeBits
  SendBunch
  WriteRecords.Enqueue


루프 1회전 = packet 한 통 분량
Writer는 채널의 스테이징 버퍼(8KB)에 쓰고, 그것이 통째로 bunch 하나가 됩니다

---

UNetConnection::FlushNet()
  LowLevelSend(SendBuffer.GetData())
    PacketHandler 체인 통과
      UIpConnection::LowLevelSend
        FSocket::SendTo
          OS의 UDP 송신 버퍼
---
---
---

Filter/Scope가 오브젝트 단위 질문
이 connection이 이 오브젝트를 받나

Conditionals
property 단위 질문
받기로 한 오브젝트에서, 이 connection은 어느 필드까지 보나

Legacy의 COND_* 가족의 Iris 구현이 FReplicationConditionals

---

NIC → OS의 UDP 수신 버퍼          ← 프레임 사이에도 쌓임 (1차 완충은 OS가)
─── 프레임 시작 ───
UNetDriver::TickDispatch          (world tick 초입에 등록된 delegate)
  IpNetDriver: Socket->RecvFrom 루프로 datagram 소진
  발신 주소 → UNetConnection 조회
  Connection->ReceivedRawPacket
    PacketHandler 수신 체인
      UNetConnection::ReceivedPacket
        bunch 분해 → 채널별 ReceivedRawBunch → ReceivedBunch → Iris Reader
          
---
---
---

UDP의 소켓은 연결이 아니라 사서함
포트 7777에 묶인 우편구 하나

OS가 해주는 일은 "발신 주소가 붙은 datagram을 이 우편구에 넣어준다"가 전부
연결이라는 개념 자체가 없습니다

TCP의 head-of-line blocking(잃어버린 packet 하나가 뒤 전부를 세움)을 피하고
신뢰성·재전송·전송 페이싱의 정책을 직접 쥐기 위해

---

그 선택의 대가로, "연결"을 유저스페이스에서 직접 지어야 합니다
UNetConnection = 유저스페이스의 TCB

TCP 커널이 연결마다 유지하는 제어 블록(TCB, Transmission Control Block)

세션의 신원(상대 주소 + 핸드셰이크로 확립된 세션), 
packet 시퀀스(OutPacketId)
수령 명단(PacketNotify — ack/nak의 원천),
신뢰성 장부, 
흐름 제어(대역폭 예산, QueuedBits, 포화 판정), 
타이머들(keepalive, timeout), 
암호화 세션(connection별 PacketHandler 체인 — 키가 세션마다 다릅니다), 
조립대(SendBuffer — packet은 세션 도장이 찍혀야 하므로), 
그리고 그 위의 다중화(채널들)

전부 "한 상대와의 대화 상태"

---

세션의 버퍼(조립 중인 SendBuffer, 부분 bunch 조립 등)는 connection 소유
커널의 datagram 버퍼(소켓의 send/recv 버퍼)는 사서함에 딸린 것이라 driver의 소켓 하나에 공유

---

UNetDriver 
역다중화기

사서함에는 모든 상대의 편지가 섞여 도착합니다. 
"이 주소는 어느 세션인가"(address → UNetConnection 해시)를 풀어 배달하는 것

그리고 미지의 주소를 세션 할당 없이 싸게 검문하는 것(stateless handshake — DDoS 방어의 핵심: 상태를 만들기 전에 거른다)

---

driver는 점점 얇아지는 중
Legacy 시절 driver의 가장 큰 내용물은 ServerReplicateActors

Iris 시대에 그 두뇌가 통째로 ReplicationSystem으로 이주

---
---
---

UDP 소켓은 관이 아닙니다. 
소포 투입구입니다

sendto(데이터, 주소) 한 번 = 완결된 datagram 하나가 원자적으로 커널에 인계됩니다

커널은 datagram마다 UDP/IP 헤더를 씌워 독립된 소포로 NIC 큐에 넣고, 
상대의 recvfrom도 정확히 소포 단위로 꺼냅니다

---

UNetDriver::TickFlush
  ReplicationSystem->NetUpdate
  for each UNetConnection: Tick
    채널들 tick
    Tick 말미: FlushNet()
      SendBuffer 완성 = packet 하나
      헤더 도장
      PacketHandler 
      Socket->SendTo(완성된 소포, 상대 주소)
  PostTickFlush
    PostSendUpdate


---
---
