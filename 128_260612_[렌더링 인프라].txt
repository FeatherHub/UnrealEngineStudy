ViewFamily = {어느 씬을, 어느 카메라로, 어느 타깃에}

머티리얼 에디터의 뷰포트 클라이언트는 
{프리뷰씬의 FScene, 프리뷰 카메라, 프리뷰 뷰포트의 RT}를 묶고

레벨 에디터의 뷰포트 클라이언트는 
{레밸 에디터 월드의 FScene, 레밸 에디터 뷰포트의 카메라, 레밸 에디터 뷰포트의 RT}를 묶습니다

---

MeshElements 의 "element"

한 번의 드로우 콜로 그릴 수 있는 최소 단위

FMeshBatch 안에 FMeshBatchElement 배열이 있고
각 element 는 인덱스 버퍼의 한 구간(FirstIndex, NumPrimitives)과 
per-draw 유니폼 버퍼를 담습니다

---

batching

"GPU 상태 변경 없이 연속으로 제출할 수 있는 작업 묶음"

---

Vertex Factory

버텍스 셰이더의 입력 단계를 만든다

머터리얼의 HLSL 코드는 "월드 포지션, 노멀, UV 를 받으면 색을 계산한다"는 계약만 알지, 
그 버텍스가 어디서 왔는지는 모릅니다

버텍스 셰이더를 위한 "버텍스 공급 방식"을 캡슐화한 것이 FVertexFactory 입니다

다양성이 존재하는 지점은 버텍스를 어떻게 얻느냐 하나뿐이고, 거기에만 추상화를 둔 겁니다

---

GPU 에 제출되는 작업의 단위는 드로우

"버텍스를 묶는다"는 건 그냥 한 버텍스 버퍼에 담는다는 뜻

---

FBatchedElements 

라인, 포인트, 스프라이트 같은 즉시 모드(immediate-mode) 프리미티브를 모아 한 번에 그리는 유틸리티

---

머터리얼은 작업이 아니라 상태입니다

머터리얼은 배칭되는 것이 아니라 배칭의 정렬 키가 되는 쪽입니다

---

UWorld ↔ FScene

UPrimitiveComponent ↔ FPrimitiveSceneProxy, 
ULightComponent ↔ FLightSceneProxy, 
UMaterialInterface ↔ FMaterialRenderProxy, 

---

"이 메시가 보이는가"라는 질문은 카메라(View) 없이는 성립하지 않습니다

---

UI 는 씬 렌더러를 통과하지 않습니다. 
Slate 가 자체 element 목록을 만들고 
FSlateElementBatcher 가 자체 배칭을 한 뒤, 
씬 렌더링이 끝난 백버퍼 위에 합성됩니다.

---

FPrimitiveSceneDesc 

컴포넌트 없이 순수 데이터만으로 프리미티브를 등록하는 경량 경로

컴포넌트 1개 = 프리미티브 1개라는 등식이 깨진다

---

ViewFamily = FSceneRenderer 한 번 실행의 입력 명세서
렌더러 한 번 실행의 출력 표면은 하나
뷰는 그 표면 위의 영역(분할 화면의 사각형, 스테레오의 좌우안)

섀도 맵은 family 당 한 번 그려서 모든 뷰가 재사용하고, 
GBuffer·뎁스 같은 중간 리소스도 한 번의 실행 수명 안에서 공유됩니다

---

Pass 의 지휘자는 렌더 스레드, GPU 는 테이프 재생기

FDeferredShadingSceneRenderer::Render() 

C++ 코드로 PrePass → ShadowDepths → BasePass → Lighting → Translucency → PostProcess 를 순서대로 호출하며 
RDG 그래프를 짓고, 

RDG 가 컴파일해 RHI 명령으로 펼치면, 
RHI 스레드는 그걸 D3D12/Vulkan 호출로 번역만 합니다. 

GPU 에 도달하는 건 평탄한 명령 테이프이고, GPU 는 앞에서부터 실행할 뿐입니다

패스의 골격 — 언제 어떤 셰이더로 어떤 타깃에 — 은 CPU 가 작성합니다

---

완성된 작업 목록을 사후에 훑으며(scan) 상태 키가 같은 항목들을 하나의 제출로 접는(merge) 것

① 상태 키로 정렬하거나 해시 버킷에 넣어 같은 것들을 인접시킨다
② 인접한 동일 키 구간을 "드로우 1회 × 인스턴스 N" 으로 접는다

데이터베이스로 말하면 GROUP BY 상태 → 작업 집계 

---

가상함수는 템플릿이 될 수 없습니다(virtual template member function 금지)

---

컴파일된 셰이더 코드는 텍스처, 버퍼, 상수를 값이 아니라 슬롯 번호(레지스터 t0, b0, s0…)로 참조합니다
"t0 에서 샘플링한다"고 적혀 있을 뿐, t0 이 무엇인지는 비어 있습니다

바인딩 = 드로우 전에 그 슬롯 표를 실제 GPU 리소스로 채우는 일입니다

PSO = 코드 + 고정 상태(불변, 비싸게 한 번 구움), 
바인딩 = 그 코드가 읽을 데이터(드로우마다 갈아끼움)

---

버텍스란 어트리뷰트의 튜플이다
위치, 노멀, UV 등이 그 어트리뷰트가 된다

---

순서대로의 루프가 아닙니다
수천 개 호출이 warp/wave 단위로 병렬 실행되고, 호출 간 순서 보장이 없습니다

---

depth prepass 와 섀도 패스는 포지션만 필요합니다
포지션이 독립 스트림이면 그 패스들은 노멀·UV 를 캐시에 끌어오지 않습니다

---

스트라이드
버텍스 i 와 i+1 사이의 바이트 거리

오프셋은 어트리뷰트별 속성입니다
한 버텍스의 데이터 블록 안에서 이 어트리뷰트가 시작하는 위치

---

오프라인(엔진/쿡 타임)에서 HLSL 은 GPU 머신코드가 아니라 이식 가능한 중간 표현으로 컴파일됩니다
D3D 는 DXIL, Vulkan 은 SPIR-V, Metal 은 AIR

진짜 머신코드(NVIDIA SASS, AMD RDNA ISA 등)로의 최종 컴파일은 런타임에 드라이버가 합니다

---

Input Assembler
고정 기능 하드웨어 스테이지

DrawIndexed(시작, 개수) 가 떨어지면, 
인덱스 버퍼를 걸어가며 각 인덱스에 대해 모든 스트림에서 해당 항목을 페치해 어트리뷰트 튜플로 합치고
그 튜플들을 VS 호출로 흘려보낸 뒤, 
VS 출력들을 토폴로지에 따라 프리미티브(삼각형 3개씩)로 묶어 
다음 스테이지에 넘깁니다

---

스트림이라는 단위 안에서는 AoS, 스트림들 사이에서는 SoA 일 수 있습니다
결정 기준은 같이 읽히는가 입니다

depth/shadow 패스가 포지션만 읽으니 포지션을 순수하게 분리하고, 
노멀과 UV 는 항상 base pass 에서 함께 읽히니 인터리브해서 한 캐시라인에 둡니다

"접근 패턴이 레이아웃을 결정한다"

---

인덱스 버퍼는 그냥 정수 열입니다 — [0,1,2,2,1,3]. 
이것만으로는 삼각형이 정의되지 않습니다. 이 열을 어떤 문법으로 읽을지가 토폴로지입니다

triangle list: 3개씩 끊어 읽음 → (0,1,2), (2,1,3) — 삼각형 2개
triangle strip: 슬라이딩 윈도우 → (0,1,2), (1,2,2), (2,2,1), (2,1,3) — 4개
line list: 2개씩 → 선분 3개
point list: 1개씩 → 점 6개.

같은 바이트 열이 토폴로지에 따라 전혀 다른 프리미티브 집합이 됩니다
즉 프리미티브 = 파서(토폴로지)를 인덱스 열에 적용한 결과

와인딩 해석은 래스터 상태

---

.usf / .ush — Unreal Shader File / Header

.usf 는 엔트리 포인트(MainVS, MainPS)를 가진 번역 단위로 컴파일 대상이고, 
.ush 는 #include 되는 헤더입니다


---

포맷 = 저장 인코딩 + 해석 규칙

UNORM 바이트 4개로 저장된 컬러는 셰이더에서 float4(0~1)로 읽힙니다. 
페치 시 변환이 일어나죠. 
즉 포맷은 "메모리에 어떻게 누워 있고, 그걸 무엇으로 해석해 셰이더에 줄 것인가" 두 가지를 함께 지정합니다

노멀을 float3(12바이트) 대신 패킹 포맷(4바이트)으로 저장하는 식의 메모리 절약이 
이 변환층 덕에 가능합니다

---

어트리뷰트 = 열(column), 
버텍스 = 행(row), 
스트림 = 열들을 묶어 따로 저장한 파일(수직 분할)

개별 버텍스 i 가 갖는 것은 말씀대로 각 스트림의 i 번째 원소

---

VertexFactory::Make(버텍스 번호 i) ⟶ 의미적 버텍스 {월드 포지션, 탄젠트 기저, UV}

예를 들어 패스 골격이 원하는 건 "버텍스 i 의 월드 포지션을 달라"입니다. 

그런데 그걸 만드는 방법이 메시 종류마다 전혀 다릅니다

LocalVF: 버퍼에서 읽고 트랜스폼 곱함. 
GPUSkinVF: 본 행렬 4개를 가중 블렌딩. 
SplineVF: 스플라인 곡선을 따라 변형. 
ParticleSpriteVF: 파티클 위치에서 사각형을 즉석 전개(버퍼에 버텍스가 아예 없음)

---

버텍스 스트림 = 버텍스의 총 어트리뷰트 중 특정 어트리뷰트 튜플들의 배열

인덱스 버퍼 
  인덱스 i 는 특정 스트림이 아니라 스키마 전체의 i 번째 행을 가리킵니다.
  토폴로지에 의해 해석되어 프리미티브로 조립

슬롯 = 메시가 선언하는 머터리얼 형식 파라미터

섹션 = 시작 인덱스 버퍼의 인덱스 + 개수 + 슬롯

---

메시 에셋 = (슬롯0, 슬롯1) 이라는 선언

에셋의 기본 머터리얼 배열 = 디폴트 인자

컴포넌트의 SetMaterial(슬롯1, 녹슨철) = 호출 시점의 오버라이드

---

전표

거래 한 건을 표준 양식에 적어 부서 간에 흘려보내는 1회용 문서
홀 직원이 "3번 테이블, 김치찌개 2" 라고 적어 주방에 넘기고, 주방은 그걸 보고 조리하고, 전표는 버려집니다
장부는 영구 기록(상태)이고, 전표는 1회용 요청(지시)입니다


참조만 담는다 — 주문서에는 음식이 아니라 "메뉴 7번"이 적혀 있습니다. 
전표에는 버텍스가 아니라 버퍼 포인터와 구간이 적혀 있고요. 
데이터 소스(주방의 재료 = 프록시의 버퍼들)는 딴 곳에 있습니다

---

GetDynamicMeshElements

프록시가 섹션별로 element 를 내보내는 것은 
머터리얼 경계에 따른 정적 분할의 반영입니다


GetDynamicMeshElements(Views, ViewFamily, VisibilityMap, Collector)

Views 가 왜 인자로 들어오는가
LOD 선택

뷰와의 거리·화면 크기를 보고 "이번엔 LOD2 의 섹션들을 전표로 쓰자"를 프록시가 정합니다
랜드스케이프 같은 거대 프리미티브는 한 발 더 나가 내부 청크 단위의 자체 컬링을 여기서 하기도 합니다

---

레벨 로드 시점

큐브 컴포넌트 2개가 Scene->AddPrimitive() 로 프록시를 만들어 등록
디렉셔널 라이트가 AddLight() 로 등록

Cached Mesh Draw Commands 


UWorld::Tick

만약 큐브가 움직였다면 MarkRenderStateDirty() 로 더러움 표시가 쌓이고, 
프레임 말 SendAllEndOfFrameUpdates() 가 그것들을 ENQUEUE_RENDER_COMMAND 로 씬에 밀어 보냅니다


UGameEngine::Tick

RedrawViewports() → UGameViewportClient::Draw() 
BeginRenderingViewFamily() 가 FDeferredShadingSceneRenderer 를 생성해 
ENQUEUE_RENDER_COMMAND 로 렌더 스레드에 던집니다


FEngineLoop::Tick
최외곽 루프

BeginFrame 마커를 렌더 스레드로 보내 프레임 경계를 표시

FFrameEndSync
게임 스레드가 렌더 스레드보다 1프레임 넘게 앞서면 여기서 블록합니다
"게임은 N, 렌더는 N−1, GPU 는 N−2" 라던 파이프라이닝의 간격을 유지하는 조절 밸브가 이 지점

---

Render Thread

던져진 FSceneRenderer::Render() 가 RDG 그래프를 지으며 진행

UpdateAllPrimitiveSceneInfos — 게임 스레드가 보낸 변경 큐를 일괄 반영
InitViews — 컬링과 선택의 폭포 상단. 프리미티브 2개의 바운드를 프러스텀에 검사
정적 메시이므로 GetDynamicMeshElements 를 부르지 않고 보관된 cached draw commands 를 꺼냅니다

---

액터 스폰 → RegisterAllComponents() → Component->RegisterComponentWithWorld(World)
이때 컴포넌트가 WorldPrivate 에 월드 포인터를 캐시합니다

---

CreateRenderState 가 UActorComponent 에 있는 이유

FScene 에 등록되는 것

ULightComponent(FLightSceneProxy), 
UDecalComponent, 
UReflectionCaptureComponent, 
포그·스카이 애트모스피어 컴포넌트

프리미티브(메시)가 아닙니다

FScene 에는 AddPrimitive 말고도 AddLight, AddDecal, … 의 Add* 가족이 있죠.

---

보통의 스태틱 메시는 프리미티브 1 = 인스턴스 1 이지만,
폴리지나 InstancedStaticMeshComponent 는 프록시(등록) 하나에 트랜스폼이 다른 복제물 수만 개입니다

프리미티브 테이블
인스턴스 테이블
PrimitiveID — 프리미티브 테이블의 행 번호

---

프리미티브는 패스의 입력입니다 — 전표를 내고 드로우가 됩니다. 
라이트는 패스의 파라미터입니다

---

World Position Offset
버텍스 스테이지의 위치 계산 사슬에 머터리얼이 끼어드는 지점입니다:

로컬 위치 → (팩토리: LocalToWorld) → 월드 위치 → (+ WPO) → 최종 월드 위치 → (View·Projection) → 클립 위치

---

토폴로지
버텍스들을 삼각형으로 묶는 규칙

그 묶음은 VS 가 끝난 뒤

---

버텍스 팩토리의 영역은 버텍스 하나 단위

---

GPUScene의 프리미티브 테이블
그 행을 PrimitiveID 로 조회해서 버텍스에 곱하는 코드가 바로 팩토리의 .ush

팩토리의 일("버텍스 i 로부터 의미적 버텍스를 만든다")
버텍스마다, GPU 에서 일어납니다
CPU 쪽 C++ 가 할 수 있는 건 설정뿐입니다

IMPLEMENT_VERTEX_FACTORY_TYPE(FLocalVertexFactory, "/Engine/Private/LocalVertexFactory.ush", ...)

---

버텍스 공급 방식 N개 × 셰이딩 코드 M개의 곱을 어떻게 끊을 것인가
정점 데이터가 다양해지는 순간(스키닝, 인스턴싱, 절차 생성) 모든 엔진이 부딪힙니다.


---

셰이더 조각의 컴파일 타임 합성, 이른바 modular shader / shader variant 시스템

---

template<class VertexSource, class Surface>    // 팩토리, 머터리얼
Pixel Pass(int i)                              // 패스 골격 (depth, shadow, base…)
{
    auto v = VertexSource::Make(i);            // 의미적 버텍스
    auto s = Surface::Describe(v);             // 표면 속성
    return Answer(v, s);                       // 패스의 질문에 답
}

---

모래시계

아래로 링크 계층이 아무리 다양해도, 위로 애플리케이션이 아무리 다양해도, 가운데 IP 하나가 양쪽을 절연합니다

아래로 버텍스 출처 N개, 위로 패스 M개, 허리는 "의미적 버텍스"라는 작은 인터페이스 하나

---

for (i : 인덱스 버퍼)        // GPU Input Assembler 고정 기능이 루프를 돈다
    v[i] = f(i)              // 팩토리가 프로그래밍하는 건 f 뿐
프리미티브 조립(v, 토폴로지) // 고정 기능


f 의 내용(버퍼에서 읽든, 본을 블렌딩하든, 절차 생성하든)은 자유

---

mesh shader 세대(D3D12 Ultimate / Vulkan, Turing·RDNA2 이후 하드웨어)
IA + VS(+ 테셀레이션 + GS) 구간을 통째로 들어내고 컴퓨트형 스테이지로 대체

mesh shader 하나는 스레드 묶음(워크그룹)이고, 출력이 버텍스 배열 + 삼각형 배열

mesh shader 워크그룹:
    원하는 곳에서 원하는 방식으로 읽고/계산해서
    out_vertices[0..V), out_triangles[0..P) 를 직접 쓴다

---

DrawIndexed(start, count)

for (n : 0 .. count)                                // 루프 구조: 고정
    i = 인덱스버퍼[start + n]                       // 페치: 고정
    if (cache.has(i)) { emit cache[i]; continue }   // 캐시: 고정 (VS 순수성이 전제)
    raw = {}
    for (attr : 버텍스선언)                         // 스키마: 설정
        raw[attr] = convert(attr.format,            // 포맷 변환: 고정
            스트림[attr.slot][ i × stride(attr.slot) + attr.offset ])
    emit cache[i] = VS(raw)                         // VS 본체: 코드
프리미티브들 = group(emitted, 토폴로지)             // 조립: 고정, 토폴로지만 설정

---

ClipVertex VS(raw)                                  
{
    prim  = GPUScene.프리미티브표[PrimitiveID]        // 조인: 버텍스별 × 객체별 데이터
    world = VF_GetWorldPosition(raw, prim)           // 코드: 팩토리(.ush) — 객체 지식
    world += Mat_WorldPositionOffset(world, time)    // 코드: 머터리얼 — 표면 지식
    return View.Proj × View.View × world             // 코드: 골격 — 뷰 지식
}

---

for (tri : 프리미티브들)                             // 고정
    if (backface(tri, 와인딩해석)) continue          // 고정, 컬모드만 설정
    for (px : tri 가 덮는 픽셀들)                    // 고정
        ctx = interpolate(tri.v₀, v₁, v₂, px)        // 고정 (보간)
        if (!depthTest(px, ctx.z)) continue          // 고정, 비교함수만 설정
        GBuffer[px] = Mat_Describe(ctx)              // 코드: 머터리얼 본체

---

세 개의 시계 — 스레드 구조

게임 스레드:   loop { simulate(N); 카메라확정; 변경→우편함; 명세서(family) 발송 }
렌더 스레드:   loop { 우편함 일괄반영; 악보작성·집행(N−1) → RHI커맨드 }
RHI/GPU:       loop { 번역·테이프재생(N−2); Present }

// 모든 통신 = 단방향 서류(큐). 공유 객체 없음 → 락 없음.
// FrameEndSync = 게임이 렌더보다 1프레임 이상 앞서면 블록 (밸브)

---

VS 출력 = ClipVertex

이후 고정 기능이 클리핑 → 원근 나눗셈(÷w, 여기서 NDC) → 뷰포트 변환(픽셀 좌표) 를 이어갑니다
VS 의 책임은 클립 공간까지

---

코드끼리의 인터페이스는 임의 이름으로, 
코드↔고정기능 인터페이스는 예약 이름으로

---

커맨드 프로세서(테이프 재생) → IA → VS → (HS→테셀레이터→DS) → (GS) → 클리핑 · ÷w · 뷰포트 변환 → 래스터라이저 → PS → OM(출력 병합기)

mesh shader 가 IA~GS 전 구간의 대체물

---

MeshElement 
섹션 {인덱스 시작, 개수}

MeshBatch 전표
element 배열
그것들이 공유하는 상태: 팩토리·머터리얼·토폴로지
참가 판정용 힌트 비트들(CastShadow, bWireframe 등)
패스 프로세서가 "이 전표를 내 패스에서 받을까" 를 판단할 때 읽는 칸

섹션
에셋 타임의 영구 기록
"머터리얼별로 나눈 것"

---

StaticMeshComponent 를 쓰는 순간, 
그 컴포넌트의 FStaticMeshSceneProxy 가 내부에서 FLocalVertexFactory 를 만들어 결선하는 것까지 다 해 줍니다

---

전제 ①: 패스 골격이 M개 있다
Depth, Shadow, Base, Velocity 전부 다른 질문이지만, 
공통으로 "버텍스 i 의 월드 위치를 달라" 고 요구합니다.

전제 ②: 월드 위치를 만드는 방법이 N개 있다
정적 버퍼에서 읽기(스태틱), 본 행렬 블렌딩(스켈레탈), 인스턴스 트랜스폼 곱(폴리지), 스플라인 변형, 파티클 즉석 전개

---

GPU 에는 가상 호출이 없다
모든 조합이 별도의 완성된 소스로 존재해야 합니다

---

섹션 
빌드가 머터리얼 경계로 인덱스 버퍼를 정렬·분할한 결과의 목차이고, {시작, 삼각형 수, 슬롯 번호} × LOD별

---

UStaticMesh
 └─ FStaticMeshRenderData          // 빌드의 산출물 (쿡되는 쪽)
     └─ FStaticMeshLODResources    // LOD 마다 하나
         ├─ VertexBuffers          // 스트림들 ← 팩토리가 결선하는 것
         ├─ IndexBuffer
         └─ Sections[]             // FStaticMeshSection

---

Stream
속도 개념

어떤 스트림은 버텍스마다 한 칸씩
어떤 스트림은 인스턴스마다 한 칸씩 전진

---

① 게임 스레드 — RegisterComponent → CreateRenderState_Concurrent
GetWorld()->Scene->AddPrimitive(this);          // 컴포넌트가 자신을 건넴 (프록시 아님)

② FScene::AddPrimitive — 아직 게임 스레드

Proxy     =  Component->CreateSceneProxy();         // 프록시 생성은 씬이 시킴
SceneInfo =  new FPrimitiveSceneInfo(Proxy, ...);   // 씬 쪽 장부 레코드로 감쌈
스냅샷    =  { 트랜스폼, 바운드, … };               // 지금 값을 복사

ENQUEUE_RENDER_COMMAND([Proxy, SceneInfo, 스냅샷] {

    // ③ 렌더 스레드 — 그런데 여기서도 바로 안 넣는다
    Proxy->CreateRenderThreadResources();        // 버퍼·팩토리 결선 등
    Scene->PrimitiveUpdates.EnqueueAdd(SceneInfo);  // 또 큐

});

④ 다음 렌더 프레임 시작 — UpdateAllPrimitiveSceneInfos 가 큐를 일괄 배수

PackedIndex = Primitives.Add(SceneInfo);         // ← 이 행 번호가 곧 PrimitiveID
PrimitiveSceneProxies[PackedIndex] = Proxy;      // 병렬 SoA 배열들
PrimitiveBounds[PackedIndex]       = …;
PrimitiveTransforms[PackedIndex]   = …;
GPUScene.업로드(PackedIndex, …);                 // GPU 거울에도 행 기입
CacheMeshDrawCommands(SceneInfo);                // 정적이면 전표 미리 결재

---

FPrimitiveSceneProxy — 컴포넌트 진영의 산물입니다. 
CreateSceneProxy() 오버라이드로 컴포넌트 종류마다 다른 파생(FStaticMeshSceneProxy...)이 만들어지고
그리는 법을 압니다

버텍스 팩토리 결선, 전표 발행(GetDynamicMeshElements), 바운드 계산. 
렌더러 모듈 밖에서 상속해 확장하라고 공개된 인터페이스

---

FPrimitiveSceneInfo — 씬 진영의 장부 레코드입니다

파생이 없는 단일 구체 클래스이고, 렌더러 모듈 내부 전용이며, 씬에서의 행정 사항을 압니다

PackedIndex(행 번호), 라이트 상호작용 목록, 캐시된 드로우 커맨드, 옥트리 등재, GPUScene 슬롯

---

class FScene : public FSceneInterface
  // Packed Array of ...
  TArray<FPrimitiveSceneInfo*> Primitives
  TArray<FPrimitiveSceneProxy*> PrimitiveSceneProxies
  TArray<FPrimitiveComponentId> PrimitiveComponentIds


class FPrimitiveSceneInfo : public FDeferredCleanupInterface
  FPrimitiveSceneProxy* Proxy
  FPrimitiveComponentId PrimitiveComponentId

---

컬링 루프가 바운드만 훑을 때 캐시라인에 바운드만 올라오게

---

"Packed"

제거할 때 빈자리를 남기지 않고 마지막 원소를 그 자리로 당겨와 메웁니다
(swap-remove — 옮겨진 원소의 PackedIndex 를 갱신)

swap-remove 때문에 PackedIndex 는 불안정합니다
PrimitiveComponentId — 안정적인 신원 키

프레임을 넘어 지속되어야 하는 기록
오클루전 쿼리 이력(이 프리미티브가 지난 프레임에 가려졌었나"

PackedIndex = 위치 키 (불안정, 빠른 배열 접근·GPUScene 행 번호용)
PrimitiveComponentId = 신원 키 (안정, 프레임 간·스레드 간 식별용)

---

GEngine->Tick 안의 RedrawViewports → BeginRenderingViewFamily 에서 ENQUEUE_RENDER_COMMAND(FDrawSceneCommand)

FEngineLoop 틱 말미에 있는 건 Slate 드로우(자기 전표를 또 큐에 넣음)와 FrameEndSync(밸브)

---

FDrawSceneCommand 실행 (렌더 스레드):
  1 우편함 배수      UpdateAllPrimitiveSceneInfos() 
    AddPrimitive 가 배열 편입을 두 번 미뤘다
    프레임 렌더링 도중에 씬 배열이 변하면 안 되기 때문

  2 가시성           InitViews: 컬링, relevance, LOD, 오클루전
    AddPrimitive 의 "스냅샷 = {트랜스폼, 바운드, …}" 
    컴포넌트의 CalcBounds() 결과

  2-2 섀도맵 구성
    섀도맵 = 광원 시점에서 본 깊이, 또 하나의 View 이다

  3-a 전표 발행      동적: GetDynamicMeshElements
                     정적: 캐시된 드로우 커맨드 꺼냄
  3-b                MeshPassProcessor (패스마다)
                     전표를 받아 → 참가 판정 → 순열 선택 → FMeshDrawCommand 로 평탄화

  4 악보 선언        GraphBuilder.AddPass(Shadow…), AddPass(Depth…),
                     AddPass(Base…), AddPass(Lighting…), AddPass(Post…)
  5 악보 실행        GraphBuilder.Execute()
                     // 그래프 컴파일: 수명 분석, 배리어 삽입, 임시 텍스처 배정
                     // 패스 람다들 호출 → RHICmdList 에 기록

  6 제출             RHI 커맨드 리스트 → RHI 스레드로 인계

---

void GetDynamicMeshElements(...) 

전표가 프레임마다 달라지는 프록시들
파티클(이번 프레임의 파티클 위치로 버텍스 데이터를 새로 씀),
절차적 메시, 디버그 드로잉, 와이어프레임 같은 뷰 모드별 분기

스태틱 메시는 등록 때 한 번(DrawStaticElements) 발행해 박제

---

FMeshBatch
공유 상태 칸: 버텍스 팩토리, 머터리얼 프록시, 토폴로지, 참가 힌트 비트
Elements[]: {인덱스 버퍼의 FirstIndex, NumPrimitives}

LOD 전환
머터리얼 교체(SetMaterial 이 프록시 포인터를 바꿈)
에디터의 선택 하이라이트나 와이어프레임 같은 뷰 모드

---

per-draw 유니폼

같은 메시, 같은 머터리얼, 같은 패스여도 드로우마다 다를 수밖에 없는 값들의 묶음

LocalToWorld
바운드, 라이팅 채널

---

① 버텍스/인덱스를 GPU 메모리에 올린다        (한 번)
② 상태를 정한다: 셰이더, 레이아웃, 블렌드    (드로우 전)
③ Draw 를 호출한다                           (매 프레임)
④ 파이프라인이 돌아 픽셀이 나온다            (GPU)

---

UStaticMesh — 공유의 축: 상자 100개가 버퍼 한 벌. (메모리 문제)

UPrimitiveComponent — 게임 플레이에 따라 변하는 트랜스폼

FSceneProxy - 게임 시뮬과 렌더링을 겹쳐 돌리기 위한 스레드 분리 장치 (병렬화)

FMeshBatch — 비싼 상태 전환를 줄이기 위해 상태 공유 단위로 묶은 요청서

FMeshDrawCommand - 캐시, 병렬, 정렬 가능한 형태

---

FMeshBatch 에서 결정되지 않는 정보

어느 셰이더 바이너리? (전표엔 머터리얼 참조만 — 패스를 모르니 순열을 못 고름)
블렌드·뎁스·컬 상태는? (머터리얼 선언 + 패스 규칙의 합성 필요)
어느 슬롯에 무엇을 바인딩?
렌더 타깃 포맷은?

FBasePassMeshProcessor::AddMeshBatch

① 참가 판정     불투명? → 통과 (반투명이었으면 여기서 끝, 출력 0개)

② 순열 선택     노랑머터리얼의 셰이더맵 [BasePass골격] [FLocalVertexFactory]
                → VS 바이너리 #A, PS 바이너리 #B          ← 미결정 1 소거

③ PSO 조립      머터리얼에서: Opaque 블렌드, 단면→백페이스컬
                 패스에서:    뎁스 Equal (프리패스가 깔아둔 값과 일치만)
                              렌더타깃 = GBuffer 4장 포맷
                 배치에서:    TriangleList
                → PSO#4217 로 캐시 조회/생성               ← 미결정 2, 4 소거

④ 바인딩 수집   b0=View유니폼, b1=Primitive유니폼 (전표의 per-draw),
                 t0… = 머터리얼 텍스처들 → 바이트 블롭으로 패킹  ← 미결정 3 소거

⑤ 기록          FMeshDrawCommand{PSO#4217, 블롭, 스트림들, IB, 0, 12, inst=1}
                → DrawListContext 에 제출

---


FMeshDrawCommand 

SetGraphicsPipelineState( PSO#4217 )           // ← 필드 1
SetUniformBuffer( b0, View ); SetUniformBuffer( b1, Primitive )
SetTexture( t0, … )                            // ← 필드 2 (블롭이 풀림)
SetStreamSource( 0, 큐브포지션VB ), …          // ← 필드 3
DrawIndexedPrimitive( IB, 0, 12, inst=1 )      // ← 필드 4

---

머터리얼이 권한자인 칸 — 표면의 성질에서 나오는 것
블렌드 모드(불투명/반투명), 양면 여부(→컬 모드). "이 표면이 무엇인가"의 귀결

패스가 권한자인 칸 — 프로토콜에서 나오는 것
뎁스 비교 함수(프리패스 후의 BasePass 는 Equal), 
렌더 타깃 개수·포맷(GBuffer 4장). "무슨 질문에 어디에 답하나"의 귀결

배치가 권한자인 칸 — 지오메트리의 사실: 토폴로지

세 부분 명세를 한 장의 평탄한 PSO 로 비정규화하는 일이 거기서만 가능

---