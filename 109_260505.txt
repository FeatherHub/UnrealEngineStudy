f : (seed, params, constraints) → content

디자이너는 "콘텐츠의 작가"에서 "콘텐츠 생성기의 작가"로

명령형 → 선언형으로의 이행


세 가지 압력

규모
변주 
룰을 바꿔서 결과 전체를 한 번에 갱신

---

작가의 의도 ↔ 자동화 자유도 (& 의도를 직접 반영하기가 어려움)
결정성 ↔ 무작위성의 풍부함 (& 예측 어려움 = 고치고 싶은 부분을 직접 고치기 어려움)
런타임 비용 ↔ 결과 품질 ( = 더 많이 계산할수록 향상)
단순한 규칙 ↔ 풍부한 출력 (& 복잡한 DSL)

---

핵심 아이디어


결정성

같은 입력 (Seed + Rule + Input Data) → 같은 출력 (Deterministic Output)
이게 깨지면 디버깅·네트워크·QA·체크포인트·shared-seed 멀티플레이가 모두 무너짐
PCG의 랜덤은 번호가 붙은 우연


계층적 노이즈

균일 노이즈는 "soup" — 어디를 봐도 똑같음 -> weighting, bias 의 필요
자연은 다중 스케일
큰 진폭의 저주파 + 절반 진폭의 2배 주파수 + 1/4 진폭의 4배 주파수 + ... = 자연스러운 텍스처


Domain warping

함수 합성이 "변주"의 본질. 단순한 함수를 자기 자신에 먹여 복잡함을 증폭. fractal과 같은 원리
noise(p + noise(p))


계층의 황금률

작은 디테일은 많고 약하게, 큰 구조는 적고 강하게
음악도 같음 — 큰 흐름이 강해야 작은 변주가 의미를 가짐


---

RNG (Random Number Generator)
특정 "Seed" 값을 입력하면 결정론적으로 예측 가능한 난수 열을 만들어냅니다


L-system, Lindenmayer System
식물의 성장 과정을 수학적으로 모델링하기 위해 고안한 시스템입니다
주로 나무, 꽃, 수풀 같은 식물 그래픽을 생성할 때 사용


GA (Genetic Algorithm)
생물의 진화 과정을 모방하여 선택, 교차, 변이 과정을 거쳐 최적의 해(콘텐츠)를 찾아냅니다
예를 들어, 특정 난이도를 만족하는 던전 구조를 진화시키는 방식에 쓰입니다.


Simulated Annealing
무작위적인 변화를 시도하되, 초기에는 큰 변화를 허용하고 
시간이 갈수록 세밀한 조정을 통해 최적의 상태로 수렴하게 만듭니다


WFC (Wave Function Collapse)
모든 가능성이 열려 있는 상태에서 인접한 타일과의 제약 조건에 따라 가능성을 하나씩 제거하며 
최종적인 패턴을 확정 짓습니다


CSP (Constraint Satisfaction Problem)
변수들이 가질 수 있는 값의 범위 내에서 모든 제약 조건을 만족하는 조합을 찾는 수학적 문제입니다


ASP (Answer Set Programming)
선언적 논리 프로그래밍의 일종
복잡한 제약 조건을 논리식으로 기술하면 컴퓨터가 이를 만족하는 해(콘텐츠 모델)를 자동으로 찾아줍니다.


GAN (Generative Adversarial Network)
생성자(Generator)와 감별자(Discriminator)라는 두 모델이 서로 경쟁


VAE (Variational Autoencoder)
입력 데이터를 압축하여 잠재 공간(Latent Space)을 학습한 뒤,
이 공간의 수치를 조정하여 새로운 데이터를 생성합니다

---
---
---

Domain
  ↓
Sample
  ↓
Point / Candidate
  ↓
Attribute / Metadata
  ↓
Filter / Rule / Constraint
  ↓
Realize
  ↓
World Object


Domain
어디에서 생성할 것인가?


Sample
후보를 어떻게 뽑을 것인가?
격자, 랜덤, Poisson disk, 텍스쳐 샘플링


Point
아직 물체는 아니지만 배치 후보
위치, 회전, 스케일, 밀도


Attribute
후보점에 붙은 의미
biome=forest, slope=0.2, wetness=0.8


Filter
무엇을 제거할 것인가?
경사 심하면 제거, 물속이면 제거, 길 근처면 제거


Transform
후보를 어떻게 변형할 것인가?
랜덤 회전, 크기 변화, 지면에 정렬


Realize
후보를 무엇으로 만들 것인가?
Static Mesh, Actor, foliage instance, decal, sound

---

여기에 집.
여기에 나무.
여기에 가로등.
여기에 벤치.

vs

상업구역은 큰 도로 근처에 둔다.
주거구역은 소음이 낮은 곳에 둔다.
공원은 물가와 인접하면 좋다.
큰 나무는 건물 입구를 막으면 안 된다.
가로등은 길을 따라 일정 간격으로 둔다.
바위는 경사면에 더 자주 둔다.


Object Thinking
  “이 나무를 어디에 둘까?”

PCG Thinking
  “나무가 생겨도 되는 장소란 무엇인가?”
  
---
---
---
---
---
---
---
---
---
---

n개의 입력축을 가진 공간  →  m개의 출력축을 가진 공간

m × n 행렬
= n차원 공간의 벡터를 받아서
  m차원 공간의 벡터로 보낸다

3 × 2 행렬 : 평면 → 3차원 공간
2 × 3 행렬 : 3차원 공간 → 평면

---

“이 변환은 n차원 부피를 몇 배로 늘리거나 줄이는가?”
“그리고 방향을 뒤집었는가?”

det = 3   → 넓이가 3배
det = 0.5 → 넓이가 절반
det = 0   → 넓이가 납작하게 붕괴
det = -2  → 넓이가 2배이고 방향이 뒤집힘

---

A = [[a, b],
     [c, d]]


첫 번째 basis vector e₁ = (1, 0) → v₁ = (a, c)
두 번째 basis vector e₂ = (0, 1) → v₂ = (b, d)

---

ad − bc

두 벡터가 만드는 “벌어짐”을 재는 값

두 벡터가 같은 방향이면 넓이는 0이어야 해요.
반대로 서로 수직에 가깝고 길면 넓이가 커져요

---

기준 단위 부피는 1

determinant는 기준 부피를 1로 잡아요

---

determinant 공식에서는 한 항이 이런 식이에요

x축 성분 하나 × y축 성분 하나 × z축 성분 하나

같은 축을 두 번 쓰면 부피가 안 생겨요.
x × x × z


그렇게 만들어진 “완전한 n차원 부피 조각”들을 더한다.

---

1. 단위 정사각형의 넓이는 1
2. 한 변을 늘리면 넓이도 선형적으로 늘어남
3. 두 변이 겹치면 넓이는 0
4. 방향이 뒤집히면 부호가 바뀜

---

행렬 A가 basis 전체를 어떻게 보내는가?

e₁, e₂, ..., eₙ
↓
Ae₁, Ae₂, ..., Aeₙ

이 새 basis 후보들이
공간 전체를 얼마나 펼치는가?

---

행렬은 basis vector들을 다른 곳으로 보내요.

이 셋은 원래 공간을 세우는 기둥이에요.

행렬이 이 기둥들을 보내고 나면 세 가지 일이 생겨요.

1. 여전히 공간을 세운다.
2. 더 크게 또는 작게 세운다.
3. 방향을 뒤집거나, 아예 납작하게 죽인다.

determinant는 그 결과를 한 숫자로 기록해요.

---

T: V → W에서 dim V, dim W

dim V < dim W인 경우 입력 큐브의 상은 W 안의 저차원 부분공간(얇은 시트)에 갇힌다
n-부피를 잴 수는 있어도, 그것을 비교할 같은 종류의 객체가 W 안에 없다. 
즉 "원래 큐브 대비 몇 배"라는 자기지시적 질문이 닫히지 않는다.


V → V일 때만 (dim V = dim W) "부피 → 부피"의 비율이 단일 스칼라로 응축된다.

---
---
---
---
---
---
---
---

FName은 FString처럼 문자열 데이터를 직접 들고 다니는 타입이 아니라, 
Unreal의 global name table에 등록된 이름을 참조하는 가벼운 값 타입에 가깝습니다


void StartAction(FName ActionName);
FName 값 하나를 복사해서 받는다.
Name table index / number 같은 작은 식별자 복사
문자열 버퍼를 복사하는 타입이 아니다

---

작은 값 타입이라면, 참조 전달이 오히려 다음 비용을 만들 수 있어요

1. 간접 참조가 생김
2. aliasing 가능성이 생김
3. 컴파일러 최적화가 덜 단순해질 수 있음
4. 코드 읽는 사람이 “이 타입이 무거운가?”라고 오해할 수 있음

---

class FName {
    uint32 ComparisonIndex;   // 전역 pool의 슬롯 번호
    uint32 Number;            // "Item_1"처럼 접미 숫자 (대부분 0)
    // editor 빌드: DisplayIndex 추가
};

trivially copyable POD. Heap 없음, 소멸자 없음, 
복사는 그냥 정수 2~3개를 register 간 이동시키는 것과 같다.

---

(1) 호출 메커니즘: x86-64 calling convention(SysV ABI 기준)에서 8바이트는 단일 레지스터(RDI 등)로 직접 전달된다. 
By-ref는 포인터를 레지스터로 전달하고, 
함수 본문에서 값을 읽으려면 dereference 한 번이 추가된다


(2) Aliasing: 컴파일러는 reference가 함수 내 다른 메모리와 alias할 가능성을 배제할 수 없다. 
그래서 값 캐싱, 명령 재배치, 레지스터 promotion 같은 최적화가 보수적으로 막힌다
By-value는 register/stack에 격리된 사본이라 aliasing이 원천 불가능

---

언제 const& 가 이기는가


TSharedPtr (atomic 참조 카운터 증감 비용) → const TSharedPtr<T>&
FString (heap-allocated TCHAR 배열) → const FString&
5+ 필드를 가진 custom struct → const FMyStruct&

---
---
---
---
---
---

BP_Action_ProjectileMagic.uasset
│
├─ UBlueprint
│  └─ 에디터용 데이터
│     그래프, 변수 목록, 노드, 컴파일 정보 등
│
├─ BP_Action_ProjectileMagic_C
│  └─ UBlueprintGeneratedClass
│     런타임에서 사용할 실제 UClass
│
└─ Default__BP_Action_ProjectileMagic_C
   └─ 이 클래스 인스턴스들의 기본값 


---

Content Browser에서 BP_Action_ProjectileMagic.asset을 선택
Blueprint asset 자체를 저장한다기보다, 그 Blueprint가 컴파일해서 만든 GeneratedClass를 저장한다


BP_Action_ProjectileMagic.uasset
    ↓ Compile
BP_Action_ProjectileMagic_C (GeneratedClass) + CDO
    ↓ NewObject
URogueActionBase runtime instance


BP_Action_ProjectileMagic_C: UBlueprintGeneratedClass
UBlueprintGeneratedClass: UClass


---

DefaultGrantActions: UClass[]
[
    BP_Action_ProjectileMagic_C,
    BP_Action_Dash_C,
    BP_Action_Heal_C
]

GrantedActions
[
    NewObject( BP_Action_ProjectileMagic_C ),
    NewObject( BP_Action_Dash_C ),
    NewObject( BP_Action_Heal_C )
]

---

Asset Registry는 각 .uasset의 primary object class를 인덱싱한다
이 자산의 primary class는 UBlueprintGeneratedClass (UClass를 상속하는)다


TObjectPtr<T> picker:
    Asset.PrimaryClass가 T의 서브클래스인 자산만 보여줘


TSubclassOf<T> picker:
    T 또는 T의 서브클래스인 모든 UClass를 보여줘
    (UBlueprintGeneratedClass와 네이티브 UClass 모두 포함)


UBlueprintGeneratedClass는 UClass이지
URogueActionBase가 아니다

---

BP_Action_ProjectileMagic.uasset
└─ UBlueprint 객체
   ├─ GeneratedClass: BP_Action_ProjectileMagic_C
   └─ CDO: Default__BP_Action_ProjectileMagic_C

---

UStaticMesh, USoundBase, UTexture2D, UMaterial

Epic이 이 클래스들에 다음을 등록해놓았다

전용 UFactory 서브클래스 (UStaticMeshFactoryNew, USoundFactory, UTextureFactory 등)
FAssetTypeActions / FAssetDefinition (UE5의 신규 시스템)
Cooker 통합

---

Pure data 객체 (UStaticMesh, UTexture, UCurveFloat, USoundWave, UDataTable, UDataAsset)

한 번 만들어진 데이터를 여러 곳이 공유 참조한다
100개의 큐브 액터가 모두 같은 SM_Cube를 가리킨다 — 각자 사본을 가질 이유가 없다
본질적으로 정체성 없는(identity-less) 데이터 묶음
비트 단위로 같다면, 같은 자산이다.


Behavior 객체 (URogueActionBase, UActorComponent 서브클래스, Pawn 등):

각 사용자가 자기 고유 인스턴스를 가져야 한다
Action은 쿨다운 타이머, 누적 데미지 같은 per-instance 상태를 가진다
캐릭터 A의 ProjectileMagic과 캐릭터 B의 ProjectileMagic은 본질적으로 다른 객체여야 한다
같은 비트 패턴이라도 다른 정체성


---

TSubclassOf<URogueActionBase> ActionClass;

URogueActionBase 객체/인스턴스를 담는 변수가 아니라
URogueActionBase 또는 그 자식 클래스를 나타내는 Reflection Data인 UClass*를 담는 변수이다


UClass*를 하나 저장하겠다.

단, 그 UClass는
URogueActionBase::StaticClass() 이거나
그 자식 UClass 클래스여야 한다.


"이 클래스로 객체를 만들면,
그 객체를 URogueActionBase*로 다룰 수 있나요?"

---

BP_Action_ProjectileMagic_C는 UClass 객체

UClass 객체가 표현하는 타입의 상속 관계

BP_Action_ProjectileMagic_C
→ URogueActionBase
→ UObject


BP_Action_ProjectileMagic_C->IsChildOf(URogueActionBase::StaticClass()) // true

---

BP_Action_ProjectileMagic_C 객체 자체는 무슨 객체인가?
UBlueprintGeneratedClass 객체다


BP_Action_ProjectileMagic_C가 표현하는 클래스는 누구를 상속하는가?
URogueActionBase를 상속한 클래스다

---

UBlueprintGeneratedClass : UClass : UStruct : UField : UObject

---

UClass 인스턴스들 사이의 관계
각 UClass 객체는 SuperStruct라는 포인터 필드를 가진다
그 포인터가 부모 클래스를 표현하는 다른 UClass 인스턴스를 가리킨다


BP_Action_ProjectileMagic_C   ← (UBlueprintGeneratedClass 타입의 객체)
   .SuperStruct → URogueActionBase의 UClass 인스턴스
        .SuperStruct → UObject의 UClass 인스턴스
             .SuperStruct → nullptr

---

BP_Action_ProjectileMagic_C

그 객체 자체의 C++ 타입: UBlueprintGeneratedClass 
그 객체가 표현하는 클래스

---

class Derived : public Base { int extra; };
// 메모리: [Base 부분][extra]
// Base* p = &derived; 가 안전한 이유.

---

NewObject<URogueActionBase>(this, BPClass)이 실행될 때:

1 
BPClass->GetStructureSize()로 전체 크기 (C++ 부모 부분 + BP 추가분) 조회 후 메모리 할당

2
C++ 부모 클래스의 생성자가 실제로 호출된다 — URogueActionBase::URogueActionBase()가 그 메모리의 앞부분을 초기화 (vptr 셋업 포함)

3
CDO에서 BP-추가 멤버들의 default 값을 복사

4
URogueActionBase*로 반환

---

"BP는 자기만의 vtable을 가지나?" 
답은 아니다. BP 클래스는 가장 가까운 C++ 조상의 vtable을 그대로 공유한다


"BP에서 함수 override는 어떻게?"
그건 vtable이 아니라 UFunction (BlueprintNativeEvent 등) 을 통해서
